-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ BLACKZ VIP FLY v6.0 | BYPASS EDITION                                          ║
-- ║ Anti-Kick • Bypass Fly • Bypass Speed • Mượt hơn                              ║
-- ║ Keys: blackzfree (FREE) | blackzmid (VIP) | blackzpro (ULTRA)                 ║
-- ║ Dựa trên nghiên cứu bypass từ GitHub, Xeno Scripts, và cộng đồng              ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 01: SERVICES                                                           ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local Players              = game:GetService("Players")
local RunService           = game:GetService("RunService")
local Workspace            = game:GetService("Workspace")
local UserInputService     = game:GetService("UserInputService")
local TweenService         = game:GetService("TweenService")
local CoreGui              = game:GetService("CoreGui")
local Lighting             = game:GetService("Lighting")
local StarterGui           = game:GetService("StarterGui")
local Stats                = game:GetService("Stats")
local HttpService          = game:GetService("HttpService")
local TeleportService      = game:GetService("TeleportService")
local VirtualUser          = game:GetService("VirtualUser")
local ReplicatedStorage    = game:GetService("ReplicatedStorage")
local ContextActionService = game:GetService("ContextActionService")
local GuiService           = game:GetService("GuiService")
local HapticService        = game:GetService("HapticService")
local LogService           = game:GetService("LogService")

local LP = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 02: LOCAL CACHE (Performance)                                          ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local pcall = pcall
local xpcall = xpcall
local ipairs = ipairs
local pairs = pairs
local next = next
local type = type
local typeof = typeof
local tostring = tostring
local tonumber = tonumber
local unpack = unpack

local math_abs = math.abs
local math_ceil = math.ceil
local math_clamp = math.clamp
local math_cos = math.cos
local math_floor = math.floor
local math_huge = math.huge
local math_max = math.max
local math_min = math.min
local math_pi = math.pi
local math_rad = math.rad
local math_random = math.random
local math_sin = math.sin
local math_sqrt = math.sqrt

local table_insert = table.insert
local table_remove = table.remove
local table_concat = table.concat
local table_find = table.find
local table_sort = table.sort

local string_format = string.format
local string_lower = string.lower
local string_upper = string.upper
local string_find = string.find
local string_gsub = string.gsub
local string_sub = string.sub
local string_rep = string.rep
local string_split = string.split
local string_match = string.match

local Vector2_new = Vector2.new
local Vector2_zero = Vector2.zero
local Vector3_new = Vector3.new
local Vector3_zero = Vector3.zero
local CFrame_new = CFrame.new
local CFrame_Angles = CFrame.Angles
local CFrame_lookAt = CFrame.lookAt
local Color3_RGB = Color3.fromRGB
local Color3_new = Color3.new
local Instance_new = Instance.new
local UDim2_new = UDim2.new
local UDim_new = UDim.new

local task_wait = task.wait
local task_spawn = task.spawn
local task_delay = task.delay
local task_defer = task.defer

local os_clock = os.clock
local os_date = os.date
local os_time = os.time
local tick = tick

local KS = Enum.KeyCode
local UIT = Enum.UserInputType
local HS = Enum.HumanoidStateType
local EasingStyle = Enum.EasingStyle
local EasingDirection = Enum.EasingDirection

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 03: HIDDEN PARENT                                                      ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local HIDDEN_PARENT = CoreGui
pcall(function()
    if gethui then
        local ok, h = pcall(gethui)
        if ok and h then HIDDEN_PARENT = h end
    end
end)

local function SafeParent(gui)
    if not gui then return end
    pcall(function() gui.Parent = HIDDEN_PARENT end)
    if gui.Parent ~= HIDDEN_PARENT then
        pcall(function() gui.Parent = CoreGui end)
    end
    if not gui.Parent then
        pcall(function() gui.Parent = LP:WaitForChild("PlayerGui", 3) end)
    end
end

if syn and syn.protect_gui then
    pcall(function() syn.protect_gui = syn.protect_gui end)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 04: EXECUTOR DETECTION                                                 ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local ExecutorInfo = {
    name = "Unknown",
    hasGethui = gethui ~= nil,
    hasHookMetamethod = hookmetamethod ~= nil,
    hasGetGC = getgc ~= nil,
    hasGetConnections = getconnections ~= nil,
    hasNewCClosure = newcclosure ~= nil,
    hasSetclipboard = setclipboard ~= nil,
    hasFireTouch = firetouchinterest ~= nil,
    hasFirePrompt = fireproximityprompt ~= nil,
    hasRequest = request ~= nil or http_request ~= nil or (syn and syn.request) ~= nil,
}

pcall(function()
    if syn then ExecutorInfo.name = "Synapse X"
    elseif fluxus then ExecutorInfo.name = "Fluxus"
    elseif Delta then ExecutorInfo.name = "Delta"
    elseif Krnl then ExecutorInfo.name = "Krnl"
    elseif getexecutorname then ExecutorInfo.name = getexecutorname()
    end
end)

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 05: BYPASS ENGINE (Anti-Kick / Bypass Speed / Bypass Fly)              ║
-- ║ Dựa trên kỹ thuật từ:                                                         ║
-- ║ - Securedlinks/Bypass.com (GitHub) - Property Spoofing & Remote Blocking      ║
-- ║ - RioCorp/ShrimpGame-Bypass - Xoá anticheat handler                           ║
-- ║ - ChronixHub - Bypass anti-cheat & admin detect                               ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝

local BypassEngine = {
    enabled = true,
    antiKickEnabled = true,
    bypassSpeedEnabled = true,
    bypassFlyEnabled = true,
    originalGravity = Workspace.Gravity,
    originalWalkSpeed = 16,
    originalJumpPower = 50,
    spoofedValues = {},
    blockedRemotes = {},
    hiddenInstances = {},
    monitoredConnections = {},
    anticheatScripts = {},
}

-- 5.1: Property Spoofing - Ngăn anti-cheat phát hiện WalkSpeed/JumpPower bị sửa
-- Kỹ thuật: Hook metamethod __index và __newindex để trả về giá trị gốc
local function EnablePropertySpoofing()
    if not hookmetamethod or not newcclosure then return false end
    
    pcall(function()
        local oldIndex
        oldIndex = hookmetamethod(game, "__index", newcclosure(function(self, key)
            -- Nếu anti-cheat đọc WalkSpeed/JumpPower, trả về giá trị gốc
            if BypassEngine.antiKickEnabled and BypassEngine.spoofedValues[self] then
                local spoof = BypassEngine.spoofedValues[self]
                if key == "WalkSpeed" and spoof.walkSpeed then
                    return spoof.walkSpeed
                elseif key == "JumpPower" and spoof.jumpPower then
                    return spoof.jumpPower
                end
            end
            return oldIndex(self, key)
        end))
    end)
    
    pcall(function()
        local oldNewIndex
        oldNewIndex = hookmetamethod(game, "__newindex", newcclosure(function(self, key, value)
            -- Cho phép script của mình ghi giá trị, nhưng anti-cheat thì không
            if BypassEngine.antiKickEnabled and BypassEngine.spoofedValues[self] then
                if key == "WalkSpeed" or key == "JumpPower" then
                    -- Lưu giá trị thật để sử dụng nội bộ
                    BypassEngine.spoofedValues[self].real = BypassEngine.spoofedValues[self].real or {}
                    BypassEngine.spoofedValues[self].real[key] = value
                    return oldNewIndex(self, key, value)
                end
            end
            return oldNewIndex(self, key, value)
        end))
    end)
    
    return true
end

-- 5.2: Remote Event Blocking - Chặn các remote event đáng ngờ từ anti-cheat
-- Kỹ thuật: Hook __namecall để chặn FireServer với các remote cụ thể
local function EnableRemoteBlocking()
    if not hookmetamethod or not newcclosure then return false end
    
    local suspiciousRemoteNames = {
        "Memer", "AntiCheat", "AC", "Detect", "Kick", "Ban", "Report",
        "Suspicious", "Exploit", "Cheat", "Hack", "Violation",
        "iac-respond", "IAC", "Security", "Check", "Validate",
    }
    
    pcall(function()
        local oldNamecall
        oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
            local method = getnamecallmethod()
            
            -- Chặn FireServer đến các remote đáng ngờ
            if BypassEngine.antiKickEnabled and method == "FireServer" and typeof(self) == "Instance" then
                local name = self.Name
                for _, suspicious in ipairs(suspiciousRemoteNames) do
                    if string_find(string_lower(name), string_lower(suspicious)) then
                        return nil -- Chặn hoàn toàn
                    end
                end
            end
            
            -- Chặn Kick từ LocalScript
            if method == "Kick" and BypassEngine.antiKickEnabled then
                return nil
            end
            
            return oldNamecall(self, ...)
        end))
    end)
    
    return true
end

-- 5.3: Anti-Cheat Script Disabling - Vô hiệu hoá các script anti-cheat
-- Kỹ thuật: Xoá hoặc vô hiệu hoá các script anti-cheat trong LocalScript
local function DisableAnticheatScripts()
    if not getgc then return false end
    
    pcall(function()
        for _, obj in ipairs(getgc(true)) do
            if typeof(obj) == "Instance" and obj:IsA("LocalScript") then
                local name = string_lower(obj.Name)
                local parent = obj.Parent
                local parentName = parent and string_lower(parent.Name) or ""
                
                -- Tìm các script anti-cheat
                if string_find(name, "anticheat") or string_find(name, "anti_cheat")
                    or string_find(name, "security") or string_find(name, "detect")
                    or string_find(parentName, "anticheat") or string_find(parentName, "security") then
                    
                    table_insert(BypassEngine.anticheatScripts, obj)
                    
                    -- Vô hiệu hoá bằng cách disconnect tất cả connections
                    if getconnections then
                        local conns = getconnections(obj.Changed)
                        if conns then
                            for _, conn in ipairs(conns) do
                                pcall(function() conn:Disconnect() end)
                            end
                        end
                    end
                    
                    -- Xoá script (local only)
                    pcall(function() obj:Destroy() end)
                end
            end
        end
    end)
    
    return #BypassEngine.anticheatScripts > 0
end

-- 5.4: Bypass Speed - Sử dụng CFrame-based movement thay vì WalkSpeed
-- Kỹ thuật: Anti-cheat thường check WalkSpeed, nhưng không check CFrame
local speedBypassConn = nil
local speedBypassEnabled = false
local speedBypassValue = 50

local function EnableSpeedBypass()
    if speedBypassConn then return end
    
    speedBypassConn = RunService.Heartbeat:Connect(function()
        if not speedBypassEnabled then return end
        if not State.hrp or not State.hum then return end
        if State.hum.Health <= 0 then return end
        if State.flyEnabled then return end -- Không dùng khi đang fly
        
        local moveDir = State.hum.MoveDirection
        if moveDir.Magnitude > 0.1 then
            -- Di chuyển bằng CFrame thay vì WalkSpeed
            local baseSpeed = State.walkSpeed or 50
            local bypassMultiplier = (speedBypassValue / 16) -- So với tốc độ gốc
            local moveCF = CFrame_new(State.hrp.Position, State.hrp.Position + moveDir)
            local newPos = State.hrp.Position + moveDir * (baseSpeed * bypassMultiplier * 0.016)
            
            -- Áp dụng CFrame
            State.hrp.CFrame = CFrame_new(newPos, newPos + moveDir)
            State.hrp.AssemblyLinearVelocity = Vector3_zero
            
            -- Giữ WalkSpeed thấp để anti-cheat không phát hiện
            State.hum.WalkSpeed = 16
        end
    end)
end

local function DisableSpeedBypass()
    if speedBypassConn then
        pcall(function() speedBypassConn:Disconnect() end)
        speedBypassConn = nil
    end
end

-- 5.5: Bypass Fly - Sử dụng CFrame-based fly thay vì BodyVelocity
-- Kỹ thuật: Anti-cheat thường check BodyVelocity/BodyGyro, nhưng không check CFrame
local flyBypassConn = nil
local flyBypassEnabled = false
local flyBypassValue = 150

local function EnableFlyBypass()
    if flyBypassConn then return end
    
    flyBypassConn = RunService.RenderStepped:Connect(function(dt)
        if not flyBypassEnabled then return end
        if not State.flyEnabled then return end
        if not State.hrp or not State.hum then return end
        if State.hum.Health <= 0 then return end
        
        dt = math_min(dt, 0.05)
        local camCF = Camera.CFrame
        local lookVec = camCF.LookVector
        local rightVec = camCF.RightVector
        
        local flatLook = Vector3_new(lookVec.X, 0, lookVec.Z)
        local flatRight = Vector3_new(rightVec.X, 0, rightVec.Z)
        if flatLook.Magnitude > 0.01 then flatLook = flatLook.Unit end
        if flatRight.Magnitude > 0.01 then flatRight = flatRight.Unit end
        
        local spd = math_clamp(flyBypassValue, 1, GetTierConfig().maxFlySpeed)
        local target = Vector3_zero
        
        -- Vertical from V-Joystick
        if State.vjoyActive and math_abs(State.vjoyValue) > 0.05 then
            target = target + Vector3_new(0, 1, 0) * (State.vjoyValue * spd)
        end
        
        -- Horizontal from WASD
        local move = State.hum.MoveDirection
        if move.Magnitude > 0.1 then
            target = target + (flatLook * move.Z + flatRight * move.X) * spd
        end
        
        -- Horizontal from H-Joystick
        if State.hjoyActive and State.hjoyValue.Magnitude > 0.1 then
            local dir = (flatRight * State.hjoyValue.X + flatLook * -State.hjoyValue.Y)
            if dir.Magnitude > 0.01 then
                target = target + dir.Unit * spd
            end
        end
        
        -- Lerp mượt
        local rate = (target.Magnitude > 0.1) and State.flyAccel or State.flyBrake
        local alpha = math_clamp(rate * dt, 0, 1)
        State.flyCurrentVel = State.flyCurrentVel:Lerp(target, alpha)
        if State.flyCurrentVel.Magnitude < 0.3 then
            State.flyCurrentVel = Vector3_zero
        end
        
        -- CFrame-based movement
        local newPos = State.hrp.Position + State.flyCurrentVel * dt
        local baseLook = flatLook
        if State.flyCurrentVel.Magnitude > 5 then
            local horiz = Vector3_new(State.flyCurrentVel.X, 0, State.flyCurrentVel.Z)
            if horiz.Magnitude > 0.1 then baseLook = horiz.Unit end
        end
        
        State.hrp.CFrame = CFrame_new(newPos, newPos + baseLook)
        State.hrp.AssemblyLinearVelocity = Vector3_zero
        State.hrp.AssemblyAngularVelocity = Vector3_zero
        
        -- Không tạo BodyVelocity/BodyGyro để tránh bị phát hiện
        -- Ẩn các dấu vết vật lý
        pcall(function()
            for _, v in ipairs(State.hrp:GetChildren()) do
                if v:IsA("BodyVelocity") or v:IsA("BodyGyro") or v:IsA("LinearVelocity") or v:IsA("AlignOrientation") then
                    v:Destroy()
                end
            end
        end)
    end)
end

local function DisableFlyBypass()
    if flyBypassConn then
        pcall(function() flyBypassConn:Disconnect() end)
        flyBypassConn = nil
    end
end

-- 5.6: Anti-Kick - Ngăn chặn bị kick khỏi game
-- Kỹ thuật: Hook Kick method và block các remote kick
local function EnableAntiKick()
    -- Chặn Kick method trên Player
    pcall(function()
        if newcclosure and hookmetamethod then
            local oldKick
            oldKick = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
                local method = getnamecallmethod()
                if method == "Kick" and BypassEngine.antiKickEnabled then
                    return nil -- Block kick
                end
                return oldKick(self, ...)
            end))
        end
    end)
    
    -- Monitor kick connections
    pcall(function()
        if getconnections then
            for _, conn in ipairs(getconnections(LP.OnTeleport)) do
                pcall(function() conn:Disconnect() end)
            end
            for _, conn in ipairs(getconnections(LP.Changed)) do
                -- Không disconnect Changed vì cần thiết
            end
        end
    end)
    
    -- Monitor LogService cho kick messages
    LogService.MessageOut:Connect(function(message, messageType)
        if not BypassEngine.antiKickEnabled then return end
        local lower = string_lower(message)
        if string_find(lower, "kick") or string_find(lower, "ban")
            or string_find(lower, "disconnect") or string_find(lower, "error code") then
            -- Log nhưng không làm gì (đã chặn Kick method)
        end
    end)
    
    return true
end

-- 5.7: Tổng hợp Bypass
local function EnableAllBypass()
    BypassEngine.enabled = true
    EnablePropertySpoofing()
    EnableRemoteBlocking()
    DisableAnticheatScripts()
    EnableAntiKick()
    
    Notify("success", "🛡️ BYPASS ON", "Anti-Kick + Speed + Fly", 3)
end

local function DisableAllBypass()
    BypassEngine.enabled = false
    DisableSpeedBypass()
    DisableFlyBypass()
    Notify("warn", "🛡️ BYPASS OFF", "", 2)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 06: KEY SYSTEM                                                         ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local TierConfig = {
    FREE = {
        name = "FREE", displayName = "Free Edition",
        color = Color3_RGB(150, 150, 150),
        maxFlySpeed = 80, allowAltitudeLock = false, allowTheme = false,
        allowInvisible = false, allowSpinbot = false, allowAimbot = false,
        allowESP = false, allowTeleport = false, allowServerHop = false,
    },
    VIP = {
        name = "VIP", displayName = "VIP Edition",
        color = Color3_RGB(0, 220, 255),
        maxFlySpeed = 300, allowAltitudeLock = true, allowTheme = true,
        allowInvisible = true, allowSpinbot = true, allowAimbot = false,
        allowESP = true, allowTeleport = true, allowServerHop = true,
    },
    ULTRA = {
        name = "ULTRA VIP", displayName = "Ultra VIP Edition",
        color = Color3_RGB(255, 100, 200),
        maxFlySpeed = 9999, allowAltitudeLock = true, allowTheme = true,
        allowInvisible = true, allowSpinbot = true, allowAimbot = true,
        allowESP = true, allowTeleport = true, allowServerHop = true,
    },
}

local KeyDatabase = {
    ["blackzfree"] = { tier = "FREE" },
    ["blackzmid"]  = { tier = "VIP" },
    ["blackzpro"]  = { tier = "ULTRA" },
}

local KeyState = { verified = false, tier = "FREE", key = nil }

local function VerifyKey(inputKey)
    if not inputKey or inputKey == "" then return false, "Vui lòng nhập key" end
    inputKey = string_lower(inputKey):gsub("^%s+", ""):gsub("%s+$", "")
    local db = KeyDatabase[inputKey]
    if not db then return false, "Key không tồn tại" end
    KeyState.verified = true
    KeyState.tier = db.tier
    KeyState.key = inputKey
    return true, "Xác thực thành công"
end

local function GetTierConfig()
    return TierConfig[KeyState.tier] or TierConfig.FREE
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 07: THEME                                                              ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local Theme = {
    accent = Color3_RGB(0, 220, 255),
    accent2 = Color3_RGB(255, 0, 200),
    bg = Color3_RGB(12, 12, 18),
    bgMid = Color3_RGB(20, 20, 28),
    bgCard = Color3_RGB(26, 26, 36),
    bgTrk = Color3_RGB(40, 40, 52),
    txt = Color3_RGB(240, 240, 250),
    sub = Color3_RGB(140, 140, 170),
    success = Color3_RGB(80, 255, 120),
    warn = Color3_RGB(255, 180, 40),
    error = Color3_RGB(255, 60, 90),
}

local ThemePresets = {
    { name = "Cyber Cyan", c1 = Color3_RGB(0, 220, 255), c2 = Color3_RGB(255, 0, 200) },
    { name = "Neon Green", c1 = Color3_RGB(80, 255, 120), c2 = Color3_RGB(0, 200, 100) },
    { name = "Blood Red",  c1 = Color3_RGB(255, 40, 60),  c2 = Color3_RGB(180, 0, 40) },
    { name = "Royal Purp", c1 = Color3_RGB(180, 100, 255), c2 = Color3_RGB(100, 0, 200) },
    { name = "Sunset",     c1 = Color3_RGB(255, 140, 40), c2 = Color3_RGB(255, 60, 90) },
    { name = "Gold Elite", c1 = Color3_RGB(255, 200, 40), c2 = Color3_RGB(255, 140, 0) },
    { name = "Ice Blue",   c1 = Color3_RGB(100, 200, 255), c2 = Color3_RGB(0, 100, 200) },
    { name = "Pink Kawaii",c1 = Color3_RGB(255, 150, 200), c2 = Color3_RGB(255, 100, 150) },
    { name = "Mint",       c1 = Color3_RGB(150, 255, 200), c2 = Color3_RGB(80, 200, 150) },
    { name = "Crimson",    c1 = Color3_RGB(220, 20, 60),  c2 = Color3_RGB(139, 0, 0) },
    { name = "Toxic",      c1 = Color3_RGB(180, 255, 0),  c2 = Color3_RGB(0, 200, 0) },
    { name = "Midnight",   c1 = Color3_RGB(50, 50, 150),  c2 = Color3_RGB(20, 20, 80) },
}

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 08: STATE + FORWARD DECLARATIONS                                       ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local State = {
    char = nil, hrp = nil, hum = nil,
    flyEnabled = false,
    flyMode = "CFrameBypass", -- Mặc định dùng CFrameBypass
    flySpeed = 150, flyAccel = 15, flyBrake = 25,
    flyAltLock = false, flyAltHeight = 100,
    flyCurrentVel = Vector3_zero,
    flyBodyVel = nil, flyBodyGyro = nil,
    flyLinearVel = nil, flyAlignOrient = nil,
    flyTargetPos = nil, flyTargetEntity = nil, flyTweenConn = nil,
    vjoyEnabled = true, vjoyValue = 0, vjoyActive = false,
    hjoyEnabled = true, hjoyValue = Vector2_zero, hjoyActive = false,
    walkSpeed = 50, jumpPower = 100,
    infiniteJump = false, antiAFK = false, noclip = false,
    speedBoost = false, speedBoostMul = 2,
    fullbright = false, invisible = false, xray = false,
    esp = false, espNames = false, espObjects = {},
    spinbot = false, spinbotSpeed = 20,
    characterSpin = false, characterSpinSpeed = 10,
    aimbot = false, aimbotRadius = 200, aimbotTarget = "Head",
    autoClick = false, hitboxExpand = false, hitboxSize = 10,
    killAura = false, killAuraRange = 20,
    godmode = false, autoRejoin = false, fpsUnlock = false,
    noFallDamage = false,
    uiOpen = false, uiMinimized = false,
    hudEnabled = true, crosshair = false,
    gravityEnabled = false, gravityValue = 196.2,
    antiKick = true, bypassSpeed = true, bypassFly = true,
    lastClickTP = 0,
}

-- Forward declarations
local VJoyGui, VJoyBase, VJoyThumb, VJoyTouch = nil, nil, nil, nil
local HJoyGui, HJoyBase, HJoyThumb, HJoyTouch = nil, nil, nil, nil
local FloatGui = nil
local MainGui, Window, Sidebar, Content, Pill, TierBadge = nil, nil, nil, nil, nil, nil
local NotifyGui = nil
local HudGui = nil
local CrosshairGui = nil
local PlayerListGui = nil
local ESPContainer = nil
local openMenu, closeMenu, BuildUI
local StartFly, StopFly, ToggleFly
local ApplyInvisible, EnableInvisibleLoop, DisableInvisibleLoop
local ClearESP, RefreshESP, CreateESPForPlayer
local CreateVJoystick, CreateHJoystick
local Notify, SafeNotify
local MakeFloatBtn

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 09: UI UTILS                                                           ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local function mk(cls, parent, props)
    local i = Instance_new(cls)
    if props then
        for k, v in pairs(props) do
            pcall(function() i[k] = v end)
        end
    end
    if parent then pcall(function() i.Parent = parent end) end
    return i
end

local function corner(p, r)
    local c = Instance_new("UICorner")
    pcall(function() c.CornerRadius = UDim_new(0, r or 6); c.Parent = p end)
    return c
end

local function stroke(p, col, thick, trans)
    local s = Instance_new("UIStroke")
    pcall(function()
        s.Color = col or Color3_new(1, 1, 1)
        s.Thickness = thick or 1
        s.Transparency = trans or 0
        s.Parent = p
    end)
    return s
end

local function gradient(p, c1, c2, rot)
    local g = Instance_new("UIGradient")
    pcall(function()
        g.Color = ColorSequence.new(c1, c2)
        g.Rotation = rot or 0
        g.Parent = p
    end)
    return g
end

local function listLayout(p, pad)
    local l = Instance_new("UIListLayout")
    pcall(function()
        l.Padding = UDim_new(0, pad or 4)
        l.SortOrder = Enum.SortOrder.LayoutOrder
        l.Parent = p
    end)
    return l
end

local function gridLayout(p, cols, cellW, cellH, padX, padY)
    local g = Instance_new("UIGridLayout")
    pcall(function()
        g.CellSize = UDim2_new(0, cellW or 100, 0, cellH or 26)
        g.CellPadding = UDim2_new(0, padX or 4, 0, padY or 4)
        g.SortOrder = Enum.SortOrder.LayoutOrder
        g.FillDirection = Enum.FillDirection.Horizontal
        g.FillDirectionMaxCells = cols or 2
        g.Parent = p
    end)
    return g
end

local function uiPadding(p, t, l, r, b)
    local x = Instance_new("UIPadding")
    pcall(function()
        x.PaddingTop = UDim_new(0, t or 0)
        x.PaddingLeft = UDim_new(0, l or 0)
        x.PaddingRight = UDim_new(0, r or 0)
        x.PaddingBottom = UDim_new(0, b or 0)
        x.Parent = p
    end)
    return x
end

local function tween(obj, time, props)
    pcall(function()
        TweenService:Create(obj, TweenInfo.new(time or 0.2), props):Play()
    end)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 10: NOTIFICATION SYSTEM                                                ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
NotifyGui = mk("ScreenGui", nil, {
    Name = "BlackZ_Notify_" .. tostring(math_random(100000, 999999)),
    ResetOnSpawn = false, IgnoreGuiInset = true,
    DisplayOrder = 99999, ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
})
SafeParent(NotifyGui)

local nstack = {}

Notify = function(kind, title, text, dur)
    pcall(function()
        dur = dur or 3
        local colors = {
            success = Theme.success, warn = Theme.warn,
            error = Theme.error, info = Theme.accent,
        }
        local col = colors[kind] or Theme.accent

        if #nstack >= 4 then
            local old = table_remove(nstack, 1)
            pcall(function() old:Destroy() end)
        end

        local f = mk("Frame", NotifyGui, {
            Size = UDim2_new(0, 260, 0, 52),
            Position = UDim2_new(1, 10, 1, -70 - #nstack * 60),
            BackgroundColor3 = Theme.bg,
            BackgroundTransparency = 0.1,
            BorderSizePixel = 0, ZIndex = 99999,
        })
        corner(f, 10)
        local fs = stroke(f, col, 1.5, 0.3)
        gradient(fs, col, Theme.accent2, 45)

        local iconBox = mk("Frame", f, {
            Size = UDim2_new(0, 34, 0, 34),
            Position = UDim2_new(0, 10, 0.5, -17),
            BackgroundColor3 = col, BackgroundTransparency = 0.7,
            BorderSizePixel = 0, ZIndex = 100001,
        })
        corner(iconBox, 999)
        mk("TextLabel", iconBox, {
            Size = UDim2_new(1, 0, 1, 0), BackgroundTransparency = 1,
            Text = "⚡", TextSize = 16, Font = Enum.Font.GothamBold,
            TextColor3 = col, ZIndex = 100002,
        })

        local titleLbl = mk("TextLabel", f, {
            Size = UDim2_new(1, -54, 0, 17),
            Position = UDim2_new(0, 52, 0, 8),
            BackgroundTransparency = 1, Text = title or "BlackZ",
            Font = Enum.Font.GothamBold, TextSize = 11,
            TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 100000,
        })
        gradient(titleLbl, col, Theme.accent2, 0)

        mk("TextLabel", f, {
            Size = UDim2_new(1, -54, 0, 15),
            Position = UDim2_new(0, 52, 0, 26),
            BackgroundTransparency = 1, Text = text or "",
            TextColor3 = Theme.sub, Font = Enum.Font.GothamMedium,
            TextSize = 9, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 100000,
        })

        local bar = mk("Frame", f, {
            Size = UDim2_new(0, 0, 0, 2),
            Position = UDim2_new(0, 10, 1, -4),
            BackgroundColor3 = col, BorderSizePixel = 0, ZIndex = 100000,
        })
        corner(bar, 1)
        gradient(bar, col, Theme.accent2, 0)
        table_insert(nstack, f)

        tween(f, 0.35, { Position = UDim2_new(1, -280, 1, -70 - (#nstack - 1) * 60) })
        tween(bar, dur, { Size = UDim2_new(1, -20, 0, 2) })

        task_delay(dur, function()
            for i = 1, #nstack do
                if nstack[i] == f then table_remove(nstack, i) break end
            end
            if f.Parent then
                tween(f, 0.25, {
                    BackgroundTransparency = 1,
                    Position = UDim2_new(1, 10, 1, f.Position.Y.Offset),
                })
                task_wait(0.3)
                pcall(function() f:Destroy() end)
            end
        end)
    end)
end

SafeNotify = function(title, text, dur)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title or "BlackZ",
            Text = text or "",
            Duration = dur or 3,
        })
    end)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 11: CHARACTER                                                          ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local function UpdateChar()
    local c = LP.Character
    if c ~= State.char then
        State.char = c
        if c then
            State.hrp = c:FindFirstChild("HumanoidRootPart")
            State.hum = c:FindFirstChildOfClass("Humanoid")
        else
            State.hrp = nil; State.hum = nil
        end
    else
        if State.char then
            State.hrp = State.char:FindFirstChild("HumanoidRootPart")
            State.hum = State.char:FindFirstChildOfClass("Humanoid")
        end
    end
end

UpdateChar()

LP.CharacterAdded:Connect(function(newChar)
    task_wait(0.5)
    UpdateChar()
    task_wait(0.5)
    if State.invisible then
        task_spawn(function()
            for _ = 1, 10 do
                if State.char then
                    for _, v in ipairs(State.char:GetDescendants()) do
                        if v:IsA("BasePart") then
                            v.Transparency = 1
                            v.LocalTransparencyModifier = 1
                            v.CastShadow = false
                        elseif v:IsA("Decal") or v:IsA("Texture") then
                            v.Transparency = 1
                        end
                    end
                end
                task_wait(0.1)
            end
        end)
    end
    if State.noclip then
        task_spawn(function()
            for _ = 1, 20 do
                if State.char then
                    for _, p in ipairs(State.char:GetDescendants()) do
                        if p:IsA("BasePart") then p.CanCollide = false end
                    end
                end
                task_wait(0.1)
            end
        end)
    end
    if BypassEngine.enabled then
        task_wait(1)
        DisableAnticheatScripts()
    end
end)

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 12: INVISIBLE ENGINE (100% Real)                                       ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local invisibleConn = nil

ApplyInvisible = function(on)
    if not State.char then
        UpdateChar()
        if not State.char then return end
    end
    pcall(function()
        for _, v in ipairs(State.char:GetDescendants()) do
            if v:IsA("BasePart") then
                v.Transparency = on and 1 or 0
                v.LocalTransparencyModifier = on and 1 or 0
                v.CastShadow = not on
            elseif v:IsA("Decal") or v:IsA("Texture") then
                v.Transparency = on and 1 or 0
            elseif v:IsA("BillboardGui") or v:IsA("SurfaceGui") then
                v.Enabled = not on
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Smoke")
                or v:IsA("Fire") or v:IsA("Sparkles") or v:IsA("PointLight")
                or v:IsA("SpotLight") then
                v.Enabled = not on
            elseif v:IsA("Accessory") then
                local handle = v:FindFirstChild("Handle")
                if handle and handle:IsA("BasePart") then
                    handle.Transparency = on and 1 or 0
                    handle.LocalTransparencyModifier = on and 1 or 0
                end
            end
        end
        if State.hum then
            if on then
                State.hum.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
                State.hum.NameDisplayDistance = 0
                State.hum.HealthDisplayDistance = 0
                State.hum.NameOcclusion = Enum.NameOcclusion.OccludeAll
                State.hum.HealthDisplayType = Enum.HumanoidHealthDisplayType.AlwaysOff
            else
                State.hum.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.Viewer
                State.hum.NameDisplayDistance = 100
                State.hum.HealthDisplayDistance = 100
                State.hum.NameOcclusion = Enum.NameOcclusion.NoOcclusion
                State.hum.HealthDisplayType = Enum.HumanoidHealthDisplayType.DisplayWhenDamaged
            end
        end
    end)
end

EnableInvisibleLoop = function()
    if invisibleConn then return end
    invisibleConn = RunService.Heartbeat:Connect(function()
        if not State.invisible then return end
        if not State.char then UpdateChar() return end
        pcall(function()
            for _, v in ipairs(State.char:GetDescendants()) do
                if v:IsA("BasePart") then
                    v.Transparency = 1
                    v.LocalTransparencyModifier = 1
                    v.CastShadow = false
                elseif v:IsA("Decal") or v:IsA("Texture") then
                    v.Transparency = 1
                elseif v:IsA("Accessory") then
                    local h = v:FindFirstChild("Handle")
                    if h then
                        h.Transparency = 1
                        h.LocalTransparencyModifier = 1
                    end
                end
            end
        end)
    end)
end

DisableInvisibleLoop = function()
    if invisibleConn then
        pcall(function() invisibleConn:Disconnect() end)
        invisibleConn = nil
    end
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 13: ESP SYSTEM                                                         ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
ESPContainer = mk("Folder", nil, { Name = "BlackZ_ESP_" .. tostring(math_random(100000, 999999)) })
SafeParent(ESPContainer)

ClearESP = function()
    for _, obj in pairs(State.espObjects) do
        pcall(function() obj:Destroy() end)
    end
    State.espObjects = {}
end

CreateESPForPlayer = function(player)
    if player == LP then return end
    if not player.Character then return end
    if State.espObjects[player] then return end

    local head = player.Character:FindFirstChild("Head")
    if not head then return end

    if State.esp then
        local hl = mk("Highlight", ESPContainer, {
            Adornee = player.Character,
            FillColor = Color3_RGB(255, 100, 100),
            FillTransparency = 0.5,
            OutlineColor = Color3_RGB(255, 255, 255),
            OutlineTransparency = 0.3,
        })
        State.espObjects[player] = hl
    end

    if State.espNames then
        local bb = mk("BillboardGui", ESPContainer, {
            Name = "ESP_" .. player.Name,
            Adornee = head,
            Size = UDim2_new(0, 200, 0, 40),
            StudsOffset = Vector3_new(0, 3, 0),
            AlwaysOnTop = true,
            ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
        })
        local lbl = mk("TextLabel", bb, {
            Size = UDim2_new(1, 0, 1, 0),
            BackgroundTransparency = 1,
            TextColor3 = Color3_RGB(255, 100, 100),
            Font = Enum.Font.GothamBold, TextSize = 12,
            TextStrokeTransparency = 0,
            TextStrokeColor3 = Color3_RGB(0, 0, 0),
        })
        State.espObjects[player.Name .. "_lbl"] = lbl
        State.espObjects[player.Name .. "_bb"] = bb
    end
end

RefreshESP = function()
    if not State.esp and not State.espNames then
        ClearESP()
        return
    end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LP and player.Character then
            if not State.espObjects[player] and not State.espObjects[player.Name .. "_lbl"] then
                CreateESPForPlayer(player)
            end
        end
    end
    for key, obj in pairs(State.espObjects) do
        if typeof(key) == "Instance" and not key.Character then
            pcall(function() obj:Destroy() end)
            State.espObjects[key] = nil
        end
    end
end

task_spawn(function()
    while true do
        task_wait(0.5)
        RefreshESP()
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LP and player.Character then
                local lbl = State.espObjects[player.Name .. "_lbl"]
                local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                local hum = player.Character:FindFirstChildOfClass("Humanoid")
                if hrp and State.hrp and lbl then
                    local dist = math_floor((hrp.Position - State.hrp.Position).Magnitude)
                    local hp = hum and math_floor(hum.Health) or 0
                    lbl.Text = player.Name .. "\n[" .. hp .. " HP] " .. dist .. "m"
                end
            end
        end
    end
end)

Players.PlayerAdded:Connect(function(p)
    p.CharacterAdded:Connect(function()
        task_wait(0.5)
        CreateESPForPlayer(p)
    end)
end)

Players.PlayerRemoving:Connect(function(p)
    local obj = State.espObjects[p]
    if obj then pcall(function() obj:Destroy() end); State.espObjects[p] = nil end
    local bb = State.espObjects[p.Name .. "_bb"]
    if bb then
        pcall(function() bb:Destroy() end)
        State.espObjects[p.Name .. "_bb"] = nil
        State.espObjects[p.Name .. "_lbl"] = nil
    end
end)

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 14: FLY ENGINE (Multi-Mode với Bypass)                                 ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local flyNoclipConn = nil

local function EnableFlyNoclip()
    if flyNoclipConn then return end
    flyNoclipConn = RunService.Stepped:Connect(function()
        if not State.flyEnabled or not State.char then return end
        for _, p in ipairs(State.char:GetDescendants()) do
            if p:IsA("BasePart") and p.CanCollide then p.CanCollide = false end
        end
    end)
end

local function DisableFlyNoclip()
    if flyNoclipConn then
        pcall(function() flyNoclipConn:Disconnect() end)
        flyNoclipConn = nil
    end
end

local function DestroyFlyParts()
    if State.flyBodyVel then pcall(function() State.flyBodyVel:Destroy() end); State.flyBodyVel = nil end
    if State.flyBodyGyro then pcall(function() State.flyBodyGyro:Destroy() end); State.flyBodyGyro = nil end
    if State.flyLinearVel then pcall(function() State.flyLinearVel:Destroy() end); State.flyLinearVel = nil end
    if State.flyAlignOrient then pcall(function() State.flyAlignOrient:Destroy() end); State.flyAlignOrient = nil end
end

local function CreateBodyVelocityFly()
    if not State.hrp then return end
    DestroyFlyParts()
    State.flyBodyVel = mk("BodyVelocity", State.hrp, {
        Name = "BlackZ_FlyVel",
        MaxForce = Vector3_new(1e6, 1e6, 1e6),
        P = 100000, Velocity = Vector3_zero,
    })
    State.flyBodyGyro = mk("BodyGyro", State.hrp, {
        Name = "BlackZ_FlyGyro",
        MaxTorque = Vector3_new(1e6, 1e6, 1e6),
        P = 10000, CFrame = State.hrp.CFrame,
    })
end

local function CreateLinearVelocityFly()
    if not State.hrp then return end
    DestroyFlyParts()
    local attach = mk("Attachment", State.hrp, { Name = "BlackZ_FlyAttach" })
    State.flyLinearVel = mk("LinearVelocity", State.hrp, {
        Name = "BlackZ_LinearVel",
        Attachment0 = attach,
        MaxForce = 1e6,
        VelocityConstraintMode = Enum.VelocityConstraintMode.Vector,
        VectorVelocity = Vector3_zero,
    })
    State.flyAlignOrient = mk("AlignOrientation", State.hrp, {
        Name = "BlackZ_AlignOr",
        Attachment0 = attach,
        MaxTorque = 1e6,
        Responsiveness = 200,
        Mode = Enum.OrientationAlignmentMode.OneAttachment,
    })
end

local function CreateFlyPartsForMode()
    if State.flyMode == "BodyVelocity" or State.flyMode == "Camera" or State.flyMode == "Behind" then
        CreateBodyVelocityFly()
    elseif State.flyMode == "LinearVelocity" then
        CreateLinearVelocityFly()
    end
    -- CFrameBypass và Tween không cần parts
end

StartFly = function()
    if not State.hrp or not State.hum then
        UpdateChar()
        if not State.hrp or not State.hum then
            Notify("error", "Fly", "Không tìm thấy nhân vật", 2)
            return
        end
    end
    State.flyEnabled = true
    EnableFlyNoclip()
    State.flyCurrentVel = Vector3_zero

    -- Chỉ tạo parts nếu không phải bypass mode
    if State.flyMode ~= "CFrameBypass" and State.flyMode ~= "Tween" then
        CreateFlyPartsForMode()
        pcall(function()
            State.hum.PlatformStand = true
            State.hum:SetStateEnabled(HS.Ragdoll, false)
            State.hum:SetStateEnabled(HS.FallingDown, false)
        end)
    else
        -- Bypass mode: tắt PlatformStand để không bị phát hiện
        pcall(function()
            State.hum.PlatformStand = false
        end)
    end

    if State.vjoyEnabled then
        if not VJoyGui then CreateVJoystick() end
        if VJoyGui then VJoyGui.Enabled = true end
    end
    
    -- Kích hoạt bypass fly nếu đang dùng bypass mode
    if State.flyMode == "CFrameBypass" then
        BypassEngine.bypassFlyEnabled = true
        flyBypassEnabled = true
        EnableFlyBypass()
    end
    
    Notify("success", "✈️ FLY ON", "Mode: " .. State.flyMode .. " (Bypass)", 2)
end

StopFly = function()
    State.flyEnabled = false
    DisableFlyNoclip()
    DestroyFlyParts()
    flyBypassEnabled = false
    pcall(function()
        if State.hum then State.hum.PlatformStand = false end
    end)
    if VJoyGui then VJoyGui.Enabled = false end
    State.vjoyValue = 0
    State.flyTargetPos = nil
    State.flyTargetEntity = nil
    Notify("warn", "✈️ FLY OFF", "", 2)
end

ToggleFly = function()
    if State.flyEnabled then StopFly() else StartFly() end
end

-- Fly render loop cho các mode không phải bypass
RunService.RenderStepped:Connect(function(dt)
    if not State.flyEnabled then return end
    if State.flyMode == "CFrameBypass" then return end -- Bypass fly có loop riêng
    if not State.hrp or not State.hum then return end
    if State.hum.Health <= 0 then StopFly() return end

    dt = math_min(dt, 0.05)
    local camCF = Camera.CFrame
    local lookVec = camCF.LookVector
    local rightVec = camCF.RightVector

    local flatLook = Vector3_new(lookVec.X, 0, lookVec.Z)
    local flatRight = Vector3_new(rightVec.X, 0, rightVec.Z)
    if flatLook.Magnitude > 0.01 then flatLook = flatLook.Unit end
    if flatRight.Magnitude > 0.01 then flatRight = flatRight.Unit end

    local spd = math_clamp(State.flySpeed, 1, GetTierConfig().maxFlySpeed)
    local target = Vector3_zero

    if State.vjoyActive and math_abs(State.vjoyValue) > 0.05 then
        target = target + Vector3_new(0, 1, 0) * (State.vjoyValue * spd)
    end

    local move = State.hum.MoveDirection
    if move.Magnitude > 0.1 then
        target = target + (flatLook * move.Z + flatRight * move.X) * spd
    end

    if State.hjoyActive and State.hjoyValue.Magnitude > 0.1 then
        local dir = (flatRight * State.hjoyValue.X + flatLook * -State.hjoyValue.Y)
        if dir.Magnitude > 0.01 then
            target = target + dir.Unit * spd
        end
    end

    if State.flyMode == "BodyVelocity" or State.flyMode == "Camera" then
        local rate = (target.Magnitude > 0.1) and State.flyAccel or State.flyBrake
        local alpha = math_clamp(rate * dt, 0, 1)
        State.flyCurrentVel = State.flyCurrentVel:Lerp(target, alpha)
        if State.flyCurrentVel.Magnitude < 0.3 then State.flyCurrentVel = Vector3_zero end
        if not State.flyBodyVel or not State.flyBodyVel.Parent then CreateBodyVelocityFly() end
        if State.flyBodyVel then State.flyBodyVel.Velocity = State.flyCurrentVel end
        if State.flyBodyGyro then State.flyBodyGyro.CFrame = camCF end
        if State.flyMode == "Camera" then
            State.hrp.CFrame = CFrame_new(State.hrp.Position, State.hrp.Position + lookVec)
        end

    elseif State.flyMode == "LinearVelocity" then
        local rate = (target.Magnitude > 0.1) and State.flyAccel or State.flyBrake
        local alpha = math_clamp(rate * dt, 0, 1)
        State.flyCurrentVel = State.flyCurrentVel:Lerp(target, alpha)
        if State.flyCurrentVel.Magnitude < 0.3 then State.flyCurrentVel = Vector3_zero end
        if not State.flyLinearVel or not State.flyLinearVel.Parent then CreateLinearVelocityFly() end
        if State.flyLinearVel then State.flyLinearVel.VectorVelocity = State.flyCurrentVel end
        if State.flyAlignOrient then State.flyAlignOrient.CFrame = camCF end

    elseif State.flyMode == "CFrame" then
        local rate = (target.Magnitude > 0.1) and State.flyAccel or State.flyBrake
        local alpha = math_clamp(rate * dt, 0, 1)
        State.flyCurrentVel = State.flyCurrentVel:Lerp(target, alpha)
        if State.flyCurrentVel.Magnitude < 0.3 then State.flyCurrentVel = Vector3_zero end
        local newPos = State.hrp.Position + State.flyCurrentVel * dt
        if State.flyAltLock and GetTierConfig().allowAltitudeLock then
            newPos = Vector3_new(newPos.X, State.flyAltHeight, newPos.Z)
        end
        local baseLook = flatLook
        if State.flyCurrentVel.Magnitude > 5 then
            local horiz = Vector3_new(State.flyCurrentVel.X, 0, State.flyCurrentVel.Z)
            if horiz.Magnitude > 0.1 then baseLook = horiz.Unit end
        end
        State.hrp.CFrame = CFrame_new(newPos, newPos + baseLook)
        State.hrp.AssemblyLinearVelocity = Vector3_zero
        State.hrp.AssemblyAngularVelocity = Vector3_zero

    elseif State.flyMode == "Behind" then
        local nearest, minDist = nil, math_huge
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LP and p.Character then
                local hrp = p.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    local d = (hrp.Position - State.hrp.Position).Magnitude
                    if d < minDist then minDist = d; nearest = hrp end
                end
            end
        end
        if nearest then
            State.flyTargetEntity = nearest
            local behindPos = nearest.Position - nearest.CFrame.LookVector * 8 + Vector3_new(0, 3, 0)
            local dir = behindPos - State.hrp.Position
            if dir.Magnitude > 0.5 then target = dir.Unit * spd end
        end
        local rate = (target.Magnitude > 0.1) and State.flyAccel or State.flyBrake
        local alpha = math_clamp(rate * dt, 0, 1)
        State.flyCurrentVel = State.flyCurrentVel:Lerp(target, alpha)
        if State.flyCurrentVel.Magnitude < 0.3 then State.flyCurrentVel = Vector3_zero end
        if not State.flyBodyVel or not State.flyBodyVel.Parent then CreateBodyVelocityFly() end
        if State.flyBodyVel then State.flyBodyVel.Velocity = State.flyCurrentVel end
        if State.flyBodyGyro then State.flyBodyGyro.CFrame = camCF end

    elseif State.flyMode == "Tween" then
        if target.Magnitude > 0.1 then
            local newPos = State.hrp.Position + target.Unit * math_min(spd * dt, spd * 0.1)
            State.hrp.CFrame = CFrame_new(newPos, newPos + flatLook)
            State.hrp.AssemblyLinearVelocity = Vector3_zero
        end
    end
end)

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 15: V-JOYSTICK (Vertical)                                              ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
CreateVJoystick = function()
    if VJoyGui then return end
    VJoyGui = mk("ScreenGui", nil, {
        Name = "BlackZ_VJoy_" .. tostring(math_random(100000, 999999)),
        ResetOnSpawn = false, IgnoreGuiInset = true,
        DisplayOrder = 99995,
    })
    SafeParent(VJoyGui)
    VJoyGui.Enabled = false

    VJoyBase = mk("Frame", VJoyGui, {
        Size = UDim2_new(0, 60, 0, 140),
        Position = UDim2_new(1, -80, 1, -170),
        BackgroundColor3 = Theme.bg, BackgroundTransparency = 0.4,
        BorderSizePixel = 0, ZIndex = 100,
    })
    corner(VJoyBase, 30)
    stroke(VJoyBase, Theme.accent, 2, 0.3)

    VJoyThumb = mk("Frame", VJoyBase, {
        Size = UDim2_new(0, 44, 0, 44),
        Position = UDim2_new(0.5, -22, 0.5, -22),
        BackgroundColor3 = Theme.accent, BackgroundTransparency = 0.2,
        BorderSizePixel = 0, ZIndex = 101,
    })
    corner(VJoyThumb, 999)
    stroke(VJoyThumb, Color3_new(1, 1, 1), 2, 0.4)
    gradient(VJoyThumb, Theme.accent, Theme.accent2, 45)

    mk("TextLabel", VJoyThumb, {
        Size = UDim2_new(1, 0, 1, 0), BackgroundTransparency = 1,
        Text = "↕", TextColor3 = Color3_new(1, 1, 1),
        TextSize = 20, Font = Enum.Font.GothamBold, ZIndex = 102,
    })
    mk("TextLabel", VJoyBase, {
        Size = UDim2_new(1, 0, 0, 12), Position = UDim2_new(0, 0, 0, -14),
        BackgroundTransparency = 1, Text = "▲",
        TextColor3 = Color3_RGB(80, 255, 120),
        TextSize = 12, Font = Enum.Font.GothamBold, ZIndex = 102,
    })
    mk("TextLabel", VJoyBase, {
        Size = UDim2_new(1, 0, 0, 12), Position = UDim2_new(0, 0, 1, 2),
        BackgroundTransparency = 1, Text = "▼",
        TextColor3 = Color3_RGB(255, 100, 100),
        TextSize = 12, Font = Enum.Font.GothamBold, ZIndex = 102,
    })

    local function UpdateVJoy(input)
        if not VJoyBase.AbsoluteSize.Y or VJoyBase.AbsoluteSize.Y <= 0 then return end
        local center = VJoyBase.AbsolutePosition + VJoyBase.AbsoluteSize / 2
        local diffY = input.Position.Y - center.Y
        local maxDist = (VJoyBase.AbsoluteSize.Y / 2) - 22
        local clamped = math_clamp(diffY, -maxDist, maxDist)
        State.vjoyValue = -clamped / maxDist
        VJoyThumb.Position = UDim2_new(0.5, -22, 0.5, clamped - 22)
    end

    local function ResetVJoy()
        State.vjoyValue = 0
        State.vjoyActive = false
        if VJoyThumb then VJoyThumb.Position = UDim2_new(0.5, -22, 0.5, -22) end
    end

    VJoyBase.InputBegan:Connect(function(input)
        if input.UserInputType == UIT.Touch or input.UserInputType == UIT.MouseButton1 then
            VJoyTouch = input; State.vjoyActive = true; UpdateVJoy(input)
        end
    end)
    VJoyBase.InputEnded:Connect(function(input)
        if input == VJoyTouch then ResetVJoy(); VJoyTouch = nil end
    end)
    VJoyBase.InputChanged:Connect(function(input)
        if input == VJoyTouch then UpdateVJoy(input) end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == VJoyTouch then UpdateVJoy(input) end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input == VJoyTouch then ResetVJoy(); VJoyTouch = nil end
    end)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 16: H-JOYSTICK (Horizontal)                                            ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
CreateHJoystick = function()
    if HJoyGui then return end
    HJoyGui = mk("ScreenGui", nil, {
        Name = "BlackZ_HJoy_" .. tostring(math_random(100000, 999999)),
        ResetOnSpawn = false, IgnoreGuiInset = true,
        DisplayOrder = 99994,
    })
    SafeParent(HJoyGui)
    HJoyGui.Enabled = true

    HJoyBase = mk("Frame", HJoyGui, {
        Size = UDim2_new(0, 100, 0, 100),
        Position = UDim2_new(0, 20, 1, -130),
        BackgroundColor3 = Theme.bg, BackgroundTransparency = 0.4,
        BorderSizePixel = 0, ZIndex = 100,
    })
    corner(HJoyBase, 999)
    stroke(HJoyBase, Theme.accent, 2, 0.3)

    HJoyThumb = mk("Frame", HJoyBase, {
        Size = UDim2_new(0, 42, 0, 42),
        Position = UDim2_new(0.5, -21, 0.5, -21),
        BackgroundColor3 = Theme.accent, BackgroundTransparency = 0.2,
        BorderSizePixel = 0, ZIndex = 101,
    })
    corner(HJoyThumb, 999)
    stroke(HJoyThumb, Color3_new(1, 1, 1), 2, 0.4)

    mk("TextLabel", HJoyThumb, {
        Size = UDim2_new(1, 0, 1, 0), BackgroundTransparency = 1,
        Text = "✥", TextColor3 = Color3_new(1, 1, 1),
        TextSize = 20, Font = Enum.Font.GothamBold, ZIndex = 102,
    })

    local function UpdateHJoy(input)
        if not HJoyBase.AbsoluteSize.X or HJoyBase.AbsoluteSize.X <= 0 then return end
        local center = HJoyBase.AbsolutePosition + HJoyBase.AbsoluteSize / 2
        local diff = input.Position - center
        local maxDist = (HJoyBase.AbsoluteSize.X / 2) - 21
        local dist = math_min(diff.Magnitude, maxDist)
        if dist > 1 then
            local dir = diff.Unit
            State.hjoyValue = dir
            HJoyThumb.Position = UDim2_new(0.5, dir.X * dist - 21, 0.5, dir.Y * dist - 21)
        else
            State.hjoyValue = Vector2_zero
            HJoyThumb.Position = UDim2_new(0.5, -21, 0.5, -21)
        end
    end

    local function ResetHJoy()
        State.hjoyValue = Vector2_zero
        State.hjoyActive = false
        if HJoyThumb then HJoyThumb.Position = UDim2_new(0.5, -21, 0.5, -21) end
    end

    HJoyBase.InputBegan:Connect(function(input)
        if input.UserInputType == UIT.Touch or input.UserInputType == UIT.MouseButton1 then
            HJoyTouch = input; State.hjoyActive = true; UpdateHJoy(input)
        end
    end)
    HJoyBase.InputEnded:Connect(function(input)
        if input == HJoyTouch then ResetHJoy(); HJoyTouch = nil end
    end)
    HJoyBase.InputChanged:Connect(function(input)
        if input == HJoyTouch then UpdateHJoy(input) end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == HJoyTouch then UpdateHJoy(input) end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input == HJoyTouch then ResetHJoy(); HJoyTouch = nil end
    end)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 17: UI COMPONENTS                                                      ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local UI = {}

function UI.heading(parent, text)
    local h = mk("TextLabel", parent, {
        Size = UDim2_new(1, 0, 0, 18),
        BackgroundTransparency = 1, Text = "▬ " .. text,
        Font = Enum.Font.GothamBold, TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 12,
    })
    gradient(h, Theme.accent, Theme.accent2, 0)
    return h
end

function UI.gridButton(parent, text, color, callback)
    local btn = mk("TextButton", parent, {
        Size = UDim2_new(0, 115, 0, 26),
        BackgroundColor3 = color or Theme.bgCard,
        BackgroundTransparency = 0.15,
        Text = text, TextColor3 = Theme.txt,
        Font = Enum.Font.GothamSemibold, TextSize = 10,
        AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 12,
    })
    corner(btn, 5)
    local s = stroke(btn, color or Theme.accent, 1, 0.5)
    gradient(s, Theme.accent, Theme.accent2, 45)
    btn.MouseButton1Click:Connect(function()
        tween(btn, 0.08, { BackgroundTransparency = 0.5 })
        task_wait(0.08)
        tween(btn, 0.15, { BackgroundTransparency = 0.15 })
        pcall(callback)
    end)
    return btn
end

function UI.gridToggle(parent, text, default, callback)
    local btn = mk("TextButton", parent, {
        Size = UDim2_new(0, 115, 0, 26),
        BackgroundColor3 = default and Theme.accent or Theme.bgCard,
        BackgroundTransparency = default and 0.1 or 0.15,
        Text = text,
        TextColor3 = default and Color3_RGB(10, 10, 15) or Theme.txt,
        Font = Enum.Font.GothamSemibold, TextSize = 10,
        AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 12,
    })
    corner(btn, 5)
    local s = stroke(btn, default and Theme.accent or Theme.sub, 1, 0.4)
    local on = default
    btn.MouseButton1Click:Connect(function()
        on = not on
        btn.BackgroundColor3 = on and Theme.accent or Theme.bgCard
        btn.BackgroundTransparency = on and 0.1 or 0.15
        btn.TextColor3 = on and Color3_RGB(10, 10, 15) or Theme.txt
        s.Color = on and Theme.accent or Theme.sub
        pcall(callback, on)
    end)
    return btn
end

function UI.slider(parent, text, minV, maxV, defaultV, callback)
    local c = mk("Frame", parent, {
        Size = UDim2_new(1, 0, 0, 38),
        BackgroundColor3 = Theme.bgCard, BackgroundTransparency = 0.25,
        BorderSizePixel = 0, ZIndex = 11,
    })
    corner(c, 6)
    stroke(c, Theme.accent, 1, 0.6)

    mk("TextLabel", c, {
        Size = UDim2_new(1, -60, 0, 14), Position = UDim2_new(0, 8, 0, 3),
        BackgroundTransparency = 1, Text = text,
        TextColor3 = Theme.txt, Font = Enum.Font.GothamBold,
        TextSize = 10, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 12,
    })
    local valLbl = mk("TextLabel", c, {
        Size = UDim2_new(0, 50, 0, 14), Position = UDim2_new(1, -58, 0, 3),
        BackgroundTransparency = 1, Text = tostring(defaultV),
        Font = Enum.Font.GothamBlack, TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Right, ZIndex = 12,
    })
    gradient(valLbl, Theme.accent, Theme.accent2, 0)

    local track = mk("Frame", c, {
        Size = UDim2_new(1, -16, 0, 5), Position = UDim2_new(0, 8, 0, 24),
        BackgroundColor3 = Theme.bgTrk, BorderSizePixel = 0, ZIndex = 12,
    })
    corner(track, 3)

    local fill = mk("Frame", track, {
        Size = UDim2_new(math_clamp((defaultV - minV) / (maxV - minV), 0, 1), 0, 1, 0),
        BackgroundColor3 = Theme.accent, BorderSizePixel = 0, ZIndex = 13,
    })
    corner(fill, 3)
    gradient(fill, Theme.accent, Theme.accent2, 0)

    local dragging = false
    local function update(input)
        if track.AbsoluteSize.X <= 0 then return end
        local ratio = math_clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
        fill.Size = UDim2_new(ratio, 0, 1, 0)
        local v = math_floor(minV + (maxV - minV) * ratio)
        valLbl.Text = tostring(v)
        pcall(callback, v)
    end

    track.InputBegan:Connect(function(input)
        if input.UserInputType == UIT.MouseButton1 or input.UserInputType == UIT.Touch then
            dragging = true; update(input)
        end
    end)
    track.InputEnded:Connect(function() dragging = false end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == UIT.MouseMovement or input.UserInputType == UIT.Touch) then
            update(input)
        end
    end)
    return c
end

function UI.colorSwatch(parent, color, callback)
    local btn = mk("TextButton", parent, {
        Size = UDim2_new(0, 32, 0, 32),
        BackgroundColor3 = color, Text = "",
        AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 12,
    })
    corner(btn, 6)
    stroke(btn, Color3_new(1, 1, 1), 1.5, 0.5)
    btn.MouseButton1Click:Connect(function()
        tween(btn, 0.15, { Size = UDim2_new(0, 38, 0, 38) })
        task_wait(0.15)
        tween(btn, 0.15, { Size = UDim2_new(0, 32, 0, 32) })
        pcall(callback, color)
    end)
    return btn
end

function UI.dropdown(parent, text, options, default, callback)
    local c = mk("Frame", parent, {
        Size = UDim2_new(1, 0, 0, 38),
        BackgroundColor3 = Theme.bgCard, BackgroundTransparency = 0.25,
        BorderSizePixel = 0, ZIndex = 11,
    })
    corner(c, 6)
    stroke(c, Theme.accent, 1, 0.6)

    mk("TextLabel", c, {
        Size = UDim2_new(0.35, 0, 1, 0), Position = UDim2_new(0, 8, 0, 0),
        BackgroundTransparency = 1, Text = text,
        TextColor3 = Theme.txt, Font = Enum.Font.GothamBold,
        TextSize = 10, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 12,
    })

    local dropdown = mk("TextButton", c, {
        Size = UDim2_new(0.6, 0, 1, -8), Position = UDim2_new(0.38, 0, 0, 4),
        BackgroundColor3 = Theme.bgMid, Text = default,
        TextColor3 = Theme.accent, Font = Enum.Font.GothamBold,
        TextSize = 10, AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 13,
    })
    corner(dropdown, 5)

    local list = mk("Frame", c, {
        Size = UDim2_new(0.6, 0, 0, 0), Position = UDim2_new(0.38, 0, 1, 2),
        BackgroundColor3 = Theme.bgMid, BackgroundTransparency = 0.05,
        BorderSizePixel = 0, ClipsDescendants = true, Visible = false, ZIndex = 15,
    })
    corner(list, 6)
    listLayout(list, 2)
    uiPadding(list, 4, 4, 4, 4)

    for _, opt in ipairs(options) do
        local ob = mk("TextButton", list, {
            Size = UDim2_new(1, 0, 0, 22),
            BackgroundColor3 = Theme.bgCard, Text = opt,
            TextColor3 = Theme.txt, Font = Enum.Font.GothamMedium,
            TextSize = 10, AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 16,
        })
        corner(ob, 4)
        ob.MouseButton1Click:Connect(function()
            dropdown.Text = opt
            list.Visible = false
            list.Size = UDim2_new(0.6, 0, 0, 0)
            pcall(callback, opt)
        end)
    end

    dropdown.MouseButton1Click:Connect(function()
        if list.Visible then
            list.Visible = false
            list.Size = UDim2_new(0.6, 0, 0, 0)
        else
            list.Visible = true
            list.Size = UDim2_new(0.6, 0, 0, #options * 24 + 8)
        end
    end)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 18: MAIN UI                                                            ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local Pages, TabBtns, CurrentTab = {}, {}, nil

local function switchTab(id)
    CurrentTab = id
    for k, p in pairs(Pages) do p.Visible = (k == id) end
end

local function UpdateTierBadge()
    if TierBadge then
        TierBadge.Text = KeyState.tier
        TierBadge.TextColor3 = GetTierConfig().color
    end
end

BuildUI = function()
    if MainGui then pcall(function() MainGui:Destroy() end) end
    Pages = {}
    TabBtns = {}

    MainGui = mk("ScreenGui", nil, {
        Name = "BlackZ_Main_" .. tostring(math_random(100000, 999999)),
        ResetOnSpawn = false, IgnoreGuiInset = true,
        DisplayOrder = 99990, ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
    })
    SafeParent(MainGui)

    local vp = Workspace.CurrentCamera.ViewportSize
    local WIN_W = 380
    local WIN_H = math_min(560, vp.Y - 60)

    Window = mk("Frame", MainGui, {
        Size = UDim2_new(0, WIN_W, 0, WIN_H),
        Position = UDim2_new(0.5, -WIN_W / 2, 0.5, -WIN_H / 2),
        BackgroundColor3 = Theme.bg, BackgroundTransparency = 0.05,
        BorderSizePixel = 0, Visible = false,
        ClipsDescendants = true, ZIndex = 10,
    })
    corner(Window, 10)
    local ws = stroke(Window, Theme.accent, 1.5, 0.4)
    gradient(ws, Theme.accent, Theme.accent2, 45)

    local TitleBar = mk("Frame", Window, {
        Size = UDim2_new(1, 0, 0, 32),
        BackgroundColor3 = Theme.bgMid, BackgroundTransparency = 0.4,
        BorderSizePixel = 0, ZIndex = 11,
    })
    local TitleTxt = mk("TextLabel", TitleBar, {
        Size = UDim2_new(1, -100, 1, 0), Position = UDim2_new(0, 12, 0, 0),
        BackgroundTransparency = 1, Text = "⚡ BLACKZ v6.0 BYPASS",
        Font = Enum.Font.GothamBlack, TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 12,
    })
    gradient(TitleTxt, Theme.accent, Theme.accent2, 0)

    TierBadge = mk("TextLabel", TitleBar, {
        Size = UDim2_new(0, 70, 0, 16), Position = UDim2_new(1, -140, 0.5, -8),
        BackgroundColor3 = Theme.bgCard, Text = KeyState.tier,
        TextColor3 = GetTierConfig().color, Font = Enum.Font.GothamBold,
        TextSize = 9, BorderSizePixel = 0, ZIndex = 13,
    })
    corner(TierBadge, 999)

    local MinBtn = mk("TextButton", TitleBar, {
        Size = UDim2_new(0, 18, 0, 18), Position = UDim2_new(1, -68, 0.5, -9),
        BackgroundColor3 = Color3_RGB(80, 255, 120), Text = "−",
        TextColor3 = Color3_RGB(10, 10, 15), Font = Enum.Font.GothamBold,
        TextSize = 13, AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 13,
    })
    corner(MinBtn, 4)

    local CloseBtn = mk("TextButton", TitleBar, {
        Size = UDim2_new(0, 18, 0, 18), Position = UDim2_new(1, -24, 0.5, -9),
        BackgroundColor3 = Color3_RGB(255, 60, 90), Text = "×",
        TextColor3 = Color3_new(1, 1, 1), Font = Enum.Font.GothamBold,
        TextSize = 13, AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 13,
    })
    corner(CloseBtn, 4)

    local SID_W = 90
    Sidebar = mk("Frame", Window, {
        Size = UDim2_new(0, SID_W, 1, -32), Position = UDim2_new(0, 0, 0, 32),
        BackgroundColor3 = Theme.bgMid, BackgroundTransparency = 0.4,
        BorderSizePixel = 0, ZIndex = 11,
    })

    Pill = mk("Frame", Sidebar, {
        Size = UDim2_new(0, 3, 0, 22), Position = UDim2_new(0, 3, 0, 8),
        BackgroundColor3 = Theme.accent, BorderSizePixel = 0, ZIndex = 13,
    })
    corner(Pill, 2)
    gradient(Pill, Theme.accent, Theme.accent2, 90)

    Content = mk("ScrollingFrame", Window, {
        Size = UDim2_new(1, -SID_W - 8, 1, -40),
        Position = UDim2_new(0, SID_W + 4, 0, 36),
        BackgroundTransparency = 1,
        ScrollBarThickness = 2, ScrollBarImageColor3 = Theme.accent,
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        CanvasSize = UDim2_new(0, 0, 0, 0),
        BorderSizePixel = 0, ZIndex = 11,
    })
    listLayout(Content, 6)
    uiPadding(Content, 3, 4, 6, 6)

    local TABS = {
        { id = "Main", icon = "🏠", name = "Chính" },
        { id = "Bypass", icon = "🛡", name = "Bypass" },
        { id = "Fly", icon = "🕊", name = "Fly" },
        { id = "Joystick", icon = "🕹", name = "Joystick" },
        { id = "Speed", icon = "⚡", name = "Tốc độ" },
        { id = "Visual", icon = "👁", name = "Hiển thị" },
        { id = "Combat", icon = "⚔", name = "Chiến đấu" },
        { id = "Misc", icon = "⚙", name = "Khác" },
        { id = "Teleport", icon = "🌀", name = "Dịch chuyển" },
        { id = "Theme", icon = "🎨", name = "Màu" },
        { id = "Info", icon = "📌", name = "Info" },
    }

    local function mkPage(id)
        local p = mk("Frame", Content, {
            Size = UDim2_new(1, 0, 0, 0),
            AutomaticSize = Enum.AutomaticSize.Y,
            BackgroundTransparency = 1, Visible = false, ZIndex = 11,
        })
        listLayout(p, 6)
        Pages[id] = p
        return p
    end

    for order, tab in ipairs(TABS) do
        local btn = mk("TextButton", Sidebar, {
            Size = UDim2_new(1, -6, 0, 26),
            Position = UDim2_new(0, 3, 0, (order - 1) * 28 + 6),
            BackgroundTransparency = 1, ZIndex = 12,
            AutoButtonColor = false, Text = "",
        })
        corner(btn, 5)

        local iconLbl = mk("TextLabel", btn, {
            Size = UDim2_new(0, 16, 1, 0), Position = UDim2_new(0, 6, 0, 0),
            BackgroundTransparency = 1, Text = tab.icon,
            TextSize = 11, TextColor3 = Theme.txt,
            TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 13,
        })
        local lbl = mk("TextLabel", btn, {
            Size = UDim2_new(1, -24, 1, 0), Position = UDim2_new(0, 22, 0, 0),
            BackgroundTransparency = 1, Text = tab.name,
            TextColor3 = Theme.sub, Font = Enum.Font.GothamBold,
            TextSize = 9, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 13,
        })

        table_insert(TabBtns, { id = tab.id, btn = btn, lbl = lbl, icon = iconLbl })

        btn.MouseButton1Click:Connect(function()
            tween(Pill, 0.3, { Position = UDim2_new(0, 3, 0, (order - 1) * 28 + 8) })
            for _, ob in ipairs(TabBtns) do
                if ob.id ~= tab.id then
                    ob.lbl.TextColor3 = Theme.sub
                    ob.icon.TextColor3 = Theme.txt
                end
            end
            lbl.TextColor3 = Theme.accent
            iconLbl.TextColor3 = Theme.accent
            switchTab(tab.id)
        end)

        mkPage(tab.id)
    end

    if #TabBtns > 0 then
        TabBtns[1].lbl.TextColor3 = Theme.accent
        TabBtns[1].icon.TextColor3 = Theme.accent
        switchTab("Main")
    end

    -- ===== TAB MAIN =====
    local pMain = Pages.Main

    local profileCard = mk("Frame", pMain, {
        Size = UDim2_new(1, 0, 0, 70),
        BackgroundColor3 = Theme.bgCard, BackgroundTransparency = 0.25,
        BorderSizePixel = 0, ZIndex = 11,
    })
    corner(profileCard, 8)
    stroke(profileCard, Theme.accent, 1, 0.6)

    local avatarFrame = mk("Frame", profileCard, {
        Size = UDim2_new(0, 48, 0, 48), Position = UDim2_new(0, 11, 0.5, -24),
        BackgroundColor3 = Theme.bgMid, BorderSizePixel = 0, ZIndex = 12,
    })
    corner(avatarFrame, 999)
    stroke(avatarFrame, Theme.accent, 2, 0.3)
    local avatarImg = mk("ImageLabel", avatarFrame, {
        Size = UDim2_new(1, -4, 1, -4), Position = UDim2_new(0, 2, 0, 2),
        BackgroundTransparency = 1,
        Image = "rbxthumb://type=AvatarHeadShot&id=" .. tostring(LP.UserId) .. "&w=150&h=150",
        ZIndex = 13,
    })
    corner(avatarImg, 999)

    local nameLbl = mk("TextLabel", profileCard, {
        Size = UDim2_new(1, -72, 0, 16), Position = UDim2_new(0, 68, 0, 10),
        BackgroundTransparency = 1, Text = LP.DisplayName or LP.Name,
        TextColor3 = Theme.txt, Font = Enum.Font.GothamBlack,
        TextSize = 11, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 12,
    })
    gradient(nameLbl, Theme.accent, Theme.accent2, 0)

    mk("TextLabel", profileCard, {
        Size = UDim2_new(1, -72, 0, 14), Position = UDim2_new(0, 68, 0, 28),
        BackgroundTransparency = 1, Text = "👑 " .. GetTierConfig().displayName,
        TextColor3 = GetTierConfig().color, Font = Enum.Font.GothamBold,
        TextSize = 9, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 12,
    })
    mk("TextLabel", profileCard, {
        Size = UDim2_new(1, -72, 0, 14), Position = UDim2_new(0, 68, 0, 44),
        BackgroundTransparency = 1, Text = "🔑 " .. (KeyState.key or "N/A"),
        TextColor3 = Theme.sub, Font = Enum.Font.GothamMedium,
        TextSize = 9, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 12,
    })

    UI.heading(pMain, "⚡ Quick Toggles")
    local qGrid = mk("Frame", pMain, {
        Size = UDim2_new(1, 0, 0, 92), BackgroundTransparency = 1,
    })
    gridLayout(qGrid, 2, 170, 26, 4, 4)

    UI.gridToggle(qGrid, "✈️ Fly", false, function(v)
        if v and not State.flyEnabled then StartFly() end
        if not v and State.flyEnabled then StopFly() end
    end)
    UI.gridToggle(qGrid, "🛡 Bypass ON", true, function(v)
        BypassEngine.enabled = v
        if v then EnableAllBypass() else DisableAllBypass() end
    end)
    UI.gridToggle(qGrid, "👻 Noclip", false, function(v) State.noclip = v end)
    UI.gridToggle(qGrid, "🔦 Fullbright", false, function(v)
        State.fullbright = v
        Lighting.Brightness = v and 2 or 1
        Lighting.GlobalShadows = not v
    end)
    UI.gridToggle(qGrid, "😴 Anti-AFK", false, function(v) State.antiAFK = v end)
    UI.gridToggle(qGrid, "🛡 Anti-Kick", true, function(v)
        State.antiKick = v
        BypassEngine.antiKickEnabled = v
    end)

    UI.heading(pMain, "🚨 Panic Button")
    local panicBtn = mk("TextButton", pMain, {
        Size = UDim2_new(1, 0, 0, 42),
        BackgroundColor3 = Color3_RGB(255, 30, 30),
        BackgroundTransparency = 0.1, Text = "🚨 PANIC - Ẩn ngay lập tức",
        TextColor3 = Color3_new(1, 1, 1), Font = Enum.Font.GothamBlack,
        TextSize = 12, AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 12,
    })
    corner(panicBtn, 8)
    stroke(panicBtn, Color3_RGB(255, 100, 100), 2, 0.3)
    gradient(panicBtn, Color3_RGB(255, 30, 30), Color3_RGB(150, 0, 0), 90)

    panicBtn.MouseButton1Click:Connect(function()
        if closeMenu then closeMenu() end
        if State.flyEnabled then StopFly() end
        if VJoyGui then VJoyGui.Enabled = false end
        if HJoyGui then HJoyGui.Enabled = false end
        SafeNotify("🚨 Panic", "Đã ẩn menu & tắt features", 2)
    end)

    -- ===== TAB BYPASS =====
    local pBypass = Pages.Bypass

    UI.heading(pBypass, "🛡️ Hệ thống Bypass")
    
    local bypassStatusCard = mk("Frame", pBypass, {
        Size = UDim2_new(1, 0, 0, 50),
        BackgroundColor3 = BypassEngine.enabled and Color3_RGB(20, 60, 20) or Theme.bgCard,
        BackgroundTransparency = 0.25,
        BorderSizePixel = 0, ZIndex = 11,
    })
    corner(bypassStatusCard, 8)
    stroke(bypassStatusCard, BypassEngine.enabled and Color3_RGB(80, 255, 120) or Theme.sub, 1.5, 0.3)
    mk("TextLabel", bypassStatusCard, {
        Size = UDim2_new(1, -16, 1, 0), Position = UDim2_new(0, 8, 0, 0),
        BackgroundTransparency = 1,
        Text = BypassEngine.enabled and "🛡️ BYPASS ĐANG BẬT" or "⚠️ BYPASS ĐANG TẮT",
        TextColor3 = BypassEngine.enabled and Color3_RGB(80, 255, 120) or Color3_RGB(255, 100, 100),
        Font = Enum.Font.GothamBlack, TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 12,
    })

    UI.gridToggle(pBypass, "🛡 Bật Bypass Tổng", true, function(v)
        BypassEngine.enabled = v
        if v then EnableAllBypass() else DisableAllBypass() end
        -- Rebuild UI để cập nhật màu
        task_spawn(function()
            task_wait(0.1)
            if BuildUI then BuildUI() end
            if openMenu then openMenu() end
            switchTab("Bypass")
        end)
    end)
    
    UI.gridToggle(pBypass, "🚫 Anti-Kick", true, function(v)
        BypassEngine.antiKickEnabled = v
        if v then EnableAntiKick() end
        Notify(v and "success" or "warn", "Anti-Kick", v and "ON" or "OFF", 2)
    end)
    
    UI.gridToggle(pBypass, "🏃 Bypass Speed", true, function(v)
        BypassEngine.bypassSpeedEnabled = v
        if v then
            speedBypassEnabled = true
            EnableSpeedBypass()
        else
            speedBypassEnabled = false
            DisableSpeedBypass()
        end
        Notify(v and "success" or "warn", "Bypass Speed", v and "ON" or "OFF", 2)
    end)
    
    UI.gridToggle(pBypass, "✈️ Bypass Fly", true, function(v)
        BypassEngine.bypassFlyEnabled = v
        if v then
            flyBypassEnabled = true
            EnableFlyBypass()
        else
            flyBypassEnabled = false
            DisableFlyBypass()
        end
        Notify(v and "success" or "warn", "Bypass Fly", v and "ON" or "OFF", 2)
    end)
    
    UI.gridButton(pBypass, "🔍 Quét Anti-Cheat", Color3_RGB(255, 200, 40), function()
        DisableAnticheatScripts()
        Notify("info", "Quét AC", "Đã vô hiệu hoá " .. #BypassEngine.anticheatScripts .. " script", 3)
    end)
    
    UI.heading(pBypass, "📊 Trạng thái")
    local statusCard = mk("Frame", pBypass, {
        Size = UDim2_new(1, 0, 0, 80),
        BackgroundColor3 = Theme.bgCard, BackgroundTransparency = 0.25,
        BorderSizePixel = 0, ZIndex = 11,
    })
    corner(statusCard, 8)
    stroke(statusCard, Theme.accent, 1, 0.6)
    mk("TextLabel", statusCard, {
        Size = UDim2_new(1, -16, 1, -12), Position = UDim2_new(0, 8, 0, 6),
        BackgroundTransparency = 1,
        Text = string_format(
            "🛡 Bypass: %s\n" ..
            "🚫 Anti-Kick: %s\n" ..
            "🏃 Bypass Speed: %s\n" ..
            "✈️ Bypass Fly: %s\n" ..
            "🔍 AC Scripts Blocked: %d",
            BypassEngine.enabled and "ON" or "OFF",
            BypassEngine.antiKickEnabled and "ON" or "OFF",
            BypassEngine.bypassSpeedEnabled and "ON" or "OFF",
            BypassEngine.bypassFlyEnabled and "ON" or "OFF",
            #BypassEngine.anticheatScripts
        ),
        TextColor3 = Theme.txt, Font = Enum.Font.Code,
        TextSize = 9, TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top, ZIndex = 12,
    })

    -- ===== TAB FLY =====
    local pFly = Pages.Fly

    UI.heading(pFly, "✈️ Điều khiển")
    UI.gridToggle(pFly, "✈️ BẬT FLY", false, function(v)
        if v then StartFly() else StopFly() end
    end)

    UI.heading(pFly, "🎮 Chế độ bay")
    UI.dropdown(pFly, "Mode", {
        "CFrameBypass", "BodyVelocity", "LinearVelocity",
        "CFrame", "Camera", "Behind", "Tween",
    }, "CFrameBypass", function(opt)
        State.flyMode = opt
        if State.flyEnabled then
            StopFly(); task_wait(0.1); StartFly()
        end
        Notify("info", "Fly Mode", opt, 2)
    end)

    UI.heading(pFly, "📊 Thông số")
    UI.slider(pFly, "✈️ Fly Speed", 30, 500, State.flySpeed, function(v)
        local maxSpd = GetTierConfig().maxFlySpeed
        if v > maxSpd then v = maxSpd end
        State.flySpeed = v
        flyBypassValue = v
    end)
    UI.slider(pFly, "🏎️ Accel", 3, 40, State.flyAccel, function(v) State.flyAccel = v end)
    UI.slider(pFly, "🛑 Brake", 5, 60, State.flyBrake, function(v) State.flyBrake = v end)

    UI.heading(pFly, "🎯 Altitude Lock (VIP+)")
    UI.gridToggle(pFly, "🎯 Lock Độ Cao", false, function(v)
        if v and not GetTierConfig().allowAltitudeLock then
            Notify("error", "VIP Only", "Cần VIP trở lên", 3); return
        end
        State.flyAltLock = v
    end)
    UI.slider(pFly, "📏 Altitude", 10, 500, State.flyAltHeight, function(v) State.flyAltHeight = v end)

    UI.heading(pFly, "⚡ Preset nhanh")
    local presetGrid = mk("Frame", pFly, {
        Size = UDim2_new(1, 0, 0, 60), BackgroundTransparency = 1,
    })
    gridLayout(presetGrid, 2, 170, 26, 4, 4)

    UI.gridButton(presetGrid, "⚡ Fast (300)", Theme.accent, function()
        State.flySpeed = math_min(300, GetTierConfig().maxFlySpeed)
        State.flyAccel = 25; State.flyBrake = 40; flyBypassValue = State.flySpeed
    end)
    UI.gridButton(presetGrid, "🎯 Smooth (150)", Color3_RGB(80, 255, 120), function()
        State.flySpeed = math_min(150, GetTierConfig().maxFlySpeed)
        State.flyAccel = 12; State.flyBrake = 25; flyBypassValue = State.flySpeed
    end)
    UI.gridButton(presetGrid, "🐢 Slow (80)", Color3_RGB(255, 200, 40), function()
        State.flySpeed = math_min(80, GetTierConfig().maxFlySpeed)
        State.flyAccel = 8; State.flyBrake = 15; flyBypassValue = State.flySpeed
    end)
    UI.gridButton(presetGrid, "🚀 Max (500)", Color3_RGB(255, 100, 200), function()
        State.flySpeed = math_min(500, GetTierConfig().maxFlySpeed)
        State.flyAccel = 30; State.flyBrake = 50; flyBypassValue = State.flySpeed
    end)

    -- ===== TAB JOYSTICK =====
    local pJoy = Pages.Joystick

    UI.heading(pJoy, "🕹️ Joystick Ngang")
    UI.gridToggle(pJoy, "🕹️ Bật H-Joystick", true, function(v)
        State.hjoyEnabled = v
        if v then
            if not HJoyGui then CreateHJoystick() end
            if HJoyGui then HJoyGui.Enabled = true end
        else
            if HJoyGui then HJoyGui.Enabled = false end
        end
    end)

    UI.heading(pJoy, "↕️ Joystick Dọc")
    UI.gridToggle(pJoy, "↕️ Bật V-Joystick", true, function(v)
        State.vjoyEnabled = v
        if v then
            if not VJoyGui then CreateVJoystick() end
            if VJoyGui and State.flyEnabled then VJoyGui.Enabled = true end
        else
            if VJoyGui then VJoyGui.Enabled = false end
        end
    end)

    UI.heading(pJoy, "🎯 Vị trí")
    UI.gridButton(pJoy, "Reset V-Joy", Theme.accent, function()
        if VJoyBase then
            VJoyBase.Position = UDim2_new(1, -80, 1, -170)
            Notify("success", "V-Joystick", "Đã reset", 2)
        end
    end)
    UI.gridButton(pJoy, "Reset H-Joy", Theme.accent, function()
        if HJoyBase then
            HJoyBase.Position = UDim2_new(0, 20, 1, -130)
            Notify("success", "H-Joystick", "Đã reset", 2)
        end
    end)

    -- ===== TAB SPEED =====
    local pSpeed = Pages.Speed

    UI.heading(pSpeed, "🚶 Walk Speed")
    UI.slider(pSpeed, "🚶 WalkSpeed", 16, 300, State.walkSpeed, function(v)
        State.walkSpeed = v
        if State.hum and not speedBypassEnabled then
            pcall(function() State.hum.WalkSpeed = v end)
        end
    end)
    UI.slider(pSpeed, "🦘 JumpPower", 50, 300, State.jumpPower, function(v)
        State.jumpPower = v
        if State.hum then pcall(function() State.hum.JumpPower = v end) end
    end)

    UI.heading(pSpeed, "⚡ Bypass Speed")
    UI.gridToggle(pSpeed, "🏃 Bypass Speed", true, function(v)
        BypassEngine.bypassSpeedEnabled = v
        speedBypassEnabled = v
        if v then EnableSpeedBypass() else DisableSpeedBypass() end
        Notify(v and "success" or "warn", "Bypass Speed", v and "ON" or "OFF", 2)
    end)
    UI.slider(pSpeed, "🏃 Speed Value", 16, 500, 50, function(v)
        speedBypassValue = v
    end)

    UI.heading(pSpeed, "🎯 Quick Speed")
    local qSpeedGrid = mk("Frame", pSpeed, {
        Size = UDim2_new(1, 0, 0, 60), BackgroundTransparency = 1,
    })
    gridLayout(qSpeedGrid, 2, 170, 26, 4, 4)
    UI.gridButton(qSpeedGrid, "⚡ 50", Theme.accent, function() State.walkSpeed = 50; speedBypassValue = 50 end)
    UI.gridButton(qSpeedGrid, "⚡ 100", Theme.accent, function() State.walkSpeed = 100; speedBypassValue = 100 end)
    UI.gridButton(qSpeedGrid, "⚡ 200", Theme.accent, function() State.walkSpeed = 200; speedBypassValue = 200 end)
    UI.gridButton(qSpeedGrid, "⚡ 300", Theme.accent, function() State.walkSpeed = 300; speedBypassValue = 300 end)

    -- ===== TAB VISUAL =====
    local pVisual = Pages.Visual

    UI.heading(pVisual, "👁 Hiển thị")
    UI.gridToggle(pVisual, "🔦 Fullbright", false, function(v)
        State.fullbright = v
        Lighting.Brightness = v and 2 or 1
        Lighting.GlobalShadows = not v
    end)
    UI.gridToggle(pVisual, "👁 X-Ray", false, function(v) State.xray = v end)

    UI.heading(pVisual, "👻 Tàng hình 100% (VIP)")
    UI.gridToggle(pVisual, "👻 Invisible", false, function(v)
        if v and not GetTierConfig().allowInvisible then
            Notify("error", "VIP Only", "Cần VIP trở lên", 3); return
        end
        State.invisible = v
        ApplyInvisible(v)
        if v then EnableInvisibleLoop() else DisableInvisibleLoop() end
    end)

    UI.heading(pVisual, "📡 ESP (VIP)")
    UI.gridToggle(pVisual, "📡 ESP Highlight", false, function(v)
        if v and not GetTierConfig().allowESP then
            Notify("error", "VIP Only", "Cần VIP trở lên", 3); return
        end
        State.esp = v; ClearESP()
    end)
    UI.gridToggle(pVisual, "📛 ESP Names", false, function(v)
        if v and not GetTierConfig().allowESP then
            Notify("error", "VIP Only", "Cần VIP trở lên", 3); return
        end
        State.espNames = v; ClearESP()
    end)

    -- ===== TAB COMBAT =====
    local pCombat = Pages.Combat

    UI.heading(pCombat, "🎯 Aimbot (ULTRA)")
    UI.gridToggle(pCombat, "🎯 Aimbot", false, function(v)
        if v and not GetTierConfig().allowAimbot then
            Notify("error", "ULTRA Only", "Cần ULTRA VIP", 3); return
        end
        State.aimbot = v
    end)
    UI.dropdown(pCombat, "Target", { "Head", "HumanoidRootPart", "Torso", "UpperTorso" }, "Head", function(opt)
        State.aimbotTarget = opt
    end)
    UI.slider(pCombat, "Radius", 50, 500, State.aimbotRadius, function(v) State.aimbotRadius = v end)

    UI.heading(pCombat, "🔄 Spinbot (VIP)")
    UI.gridToggle(pCombat, "🔄 Spinbot", false, function(v)
        if v and not GetTierConfig().allowSpinbot then
            Notify("error", "VIP Only", "Cần VIP trở lên", 3); return
        end
        State.spinbot = v
    end)
    UI.slider(pCombat, "Spin Speed", 5, 100, State.spinbotSpeed, function(v) State.spinbotSpeed = v end)

    UI.gridToggle(pCombat, "💫 Character Spin", false, function(v)
        if v and not GetTierConfig().allowSpinbot then
            Notify("error", "VIP Only", "Cần VIP trở lên", 3); return
        end
        State.characterSpin = v
    end)
    UI.slider(pCombat, "Char Spin Speed", 1, 50, State.characterSpinSpeed, function(v) State.characterSpinSpeed = v end)

    UI.heading(pCombat, "⚔️ Chiến đấu")
    UI.gridToggle(pCombat, "⚔️ Kill Aura", false, function(v) State.killAura = v end)
    UI.slider(pCombat, "Aura Range", 5, 50, State.killAuraRange, function(v) State.killAuraRange = v end)

    UI.gridToggle(pCombat, "📏 Hitbox Expand", false, function(v) State.hitboxExpand = v end)
    UI.slider(pCombat, "Hitbox Size", 5, 30, State.hitboxSize, function(v) State.hitboxSize = v end)

    -- ===== TAB MISC =====
    local pMisc = Pages.Misc

    UI.heading(pMisc, "⚙️ General")
    UI.gridToggle(pMisc, "🛡️ Godmode", false, function(v) State.godmode = v end)
    UI.gridToggle(pMisc, "👻 Noclip", false, function(v) State.noclip = v end)
    UI.gridToggle(pMisc, "🦘 Infinite Jump", false, function(v) State.infiniteJump = v end)
    UI.gridToggle(pMisc, "😴 Anti-AFK", false, function(v) State.antiAFK = v end)
    UI.gridToggle(pMisc, "🛬 No Fall Damage", false, function(v) State.noFallDamage = v end)
    UI.gridToggle(pMisc, "🛡️ Anti-Kick", true, function(v)
        State.antiKick = v
        BypassEngine.antiKickEnabled = v
        if v then EnableAntiKick() end
    end)

    UI.heading(pMisc, "🔧 Hệ thống")
    UI.gridToggle(pMisc, "🔄 Auto Rejoin", false, function(v) State.autoRejoin = v end)
    UI.gridToggle(pMisc, "🚀 FPS Boost", false, function(v)
        State.fpsUnlock = v
        pcall(function()
            if v then
                Lighting.GlobalShadows = false
                Lighting.FogEnd = 1e9
                Lighting.Brightness = 2
                if setfpscap then setfpscap(240) end
            else
                Lighting.GlobalShadows = true
                Lighting.Brightness = 1
                if setfpscap then setfpscap(60) end
            end
        end)
    end)

    UI.heading(pMisc, "🌍 Gravity")
    UI.gridToggle(pMisc, "🌍 Custom Gravity", false, function(v)
        State.gravityEnabled = v
        if v then Workspace.Gravity = State.gravityValue else Workspace.Gravity = BypassEngine.originalGravity end
    end)
    UI.slider(pMisc, "Gravity", 50, 500, State.gravityValue, function(v)
        State.gravityValue = v
        if State.gravityEnabled then Workspace.Gravity = v end
    end)

    UI.heading(pMisc, "⚠️ Nguy hiểm")
    local dangerGrid = mk("Frame", pMisc, {
        Size = UDim2_new(1, 0, 0, 30), BackgroundTransparency = 1,
    })
    gridLayout(dangerGrid, 2, 170, 26, 4, 4)
    UI.gridButton(dangerGrid, "🔄 Reset Char", Color3_RGB(255, 100, 100), function()
        if LP.Character then LP.Character:BreakJoints() end
    end)
    UI.gridButton(dangerGrid, "🗑 Unload", Color3_RGB(255, 60, 60), function()
        _G.BlackZ_Alive = false
        if MainGui then MainGui:Destroy() end
        if NotifyGui then NotifyGui:Destroy() end
        if VJoyGui then VJoyGui:Destroy() end
        if HJoyGui then HJoyGui:Destroy() end
        if FloatGui then FloatGui:Destroy() end
        Notify("warn", "Unloaded", "Script đã tắt", 2)
    end)

    -- ===== TAB TELEPORT =====
    local pTP = Pages.Teleport

    UI.heading(pTP, "🌀 Teleport (VIP)")
    UI.gridButton(pTP, "🌍 TP Random", Theme.accent, function()
        if not GetTierConfig().allowTeleport then
            Notify("error", "VIP Only", "Cần VIP trở lên", 3); return
        end
        if State.hrp then
            local x = math_random(-500, 500)
            local z = math_random(-500, 500)
            State.hrp.CFrame = CFrame_new(Vector3_new(x, 50, z))
            Notify("success", "TP", "Đã teleport", 2)
        end
    end)
    UI.gridButton(pTP, "⬆️ TP Lên Cao", Theme.accent, function()
        if not GetTierConfig().allowTeleport then
            Notify("error", "VIP Only", "Cần VIP trở lên", 3); return
        end
        if State.hrp then State.hrp.CFrame = State.hrp.CFrame + Vector3_new(0, 500, 0) end
    end)

    UI.heading(pTP, "📍 Save Position")
    UI.gridButton(pTP, "📌 Lưu Vị Trí", Color3_RGB(80, 255, 120), function()
        if State.hrp then
            _G.BlackZ_SavedPos = State.hrp.Position
            Notify("success", "Saved", "Đã lưu vị trí", 2)
        end
    end)
    UI.gridButton(pTP, "🚀 Về Vị Trí Đã Lưu", Color3_RGB(80, 255, 120), function()
        if _G.BlackZ_SavedPos and State.hrp then
            State.hrp.CFrame = CFrame_new(_G.BlackZ_SavedPos + Vector3_new(0, 3, 0))
            Notify("success", "TP", "Về vị trí đã lưu", 2)
        else
            Notify("warn", "Chưa lưu", "Bấm lưu trước", 2)
        end
    end)

    UI.heading(pTP, "🎯 TP đến người chơi")
    local tpPlayersFrame = mk("Frame", pTP, {
        Size = UDim2_new(1, 0, 0, 200), BackgroundTransparency = 1,
    })
    listLayout(tpPlayersFrame, 4)

    task_spawn(function()
        while tpPlayersFrame.Parent do
            task_wait(2)
            for _, c in ipairs(tpPlayersFrame:GetChildren()) do
                if c:IsA("TextButton") then c:Destroy() end
            end
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= LP then
                    local btn = UI.gridButton(tpPlayersFrame, "🎯 " .. p.Name, Theme.bgCard, function()
                        if not GetTierConfig().allowTeleport then
                            Notify("error", "VIP Only", "Cần VIP", 3); return
                        end
                        if p.Character and p.Character:FindFirstChild("HumanoidRootPart") and State.hrp then
                            State.hrp.CFrame = p.Character.HumanoidRootPart.CFrame * CFrame_new(0, 3, 0)
                            Notify("success", "TP", "Đến " .. p.Name, 2)
                        end
                    end)
                    btn.Size = UDim2_new(1, 0, 0, 26)
                end
            end
        end
    end)

    -- ===== TAB THEME =====
    local pTheme = Pages.Theme

    if not GetTierConfig().allowTheme then
        UI.heading(pTheme, "🔒 Yêu cầu VIP")
        local lockCard = mk("Frame", pTheme, {
            Size = UDim2_new(1, 0, 0, 50),
            BackgroundColor3 = Theme.bgCard, BackgroundTransparency = 0.25,
            BorderSizePixel = 0, ZIndex = 11,
        })
        corner(lockCard, 8)
        mk("TextLabel", lockCard, {
            Size = UDim2_new(1, -16, 1, 0), Position = UDim2_new(0, 8, 0, 0),
            BackgroundTransparency = 1,
            Text = "🔒 Cần key blackzmid hoặc blackzpro",
            TextColor3 = Color3_RGB(255, 100, 100),
            Font = Enum.Font.GothamBold, TextSize = 10,
            TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 12,
        })
    else
        UI.heading(pTheme, "🎨 Preset Theme")
        local presetFrame = mk("Frame", pTheme, {
            Size = UDim2_new(1, 0, 0, 130), BackgroundTransparency = 1,
        })
        gridLayout(presetFrame, 4, 80, 34, 4, 4)

        for _, preset in ipairs(ThemePresets) do
            UI.colorSwatch(presetFrame, preset.c1, function(color)
                Theme.accent = preset.c1
                Theme.accent2 = preset.c2
                Notify("success", "Theme", preset.name, 2)
                task_spawn(function()
                    task_wait(0.15)
                    if BuildUI then BuildUI() end
                    if openMenu then openMenu() end
                    switchTab("Theme")
                end)
            end)
        end

        UI.heading(pTheme, "🎨 Màu tùy chỉnh RGB")
        UI.slider(pTheme, "🔴 Đỏ", 0, 255, math_floor(Theme.accent.R * 255), function(v)
            Theme.accent = Color3_RGB(v, math_floor(Theme.accent.G * 255), math_floor(Theme.accent.B * 255))
        end)
        UI.slider(pTheme, "🟢 Lục", 0, 255, math_floor(Theme.accent.G * 255), function(v)
            Theme.accent = Color3_RGB(math_floor(Theme.accent.R * 255), v, math_floor(Theme.accent.B * 255))
        end)
        UI.slider(pTheme, "🔵 Lam", 0, 255, math_floor(Theme.accent.B * 255), function(v)
            Theme.accent = Color3_RGB(math_floor(Theme.accent.R * 255), math_floor(Theme.accent.G * 255), v)
        end)

        UI.gridButton(pTheme, "✅ Áp dụng màu RGB", Theme.accent, function()
            Notify("success", "Theme", "Đã áp dụng màu mới", 2)
            task_spawn(function()
                if BuildUI then BuildUI() end
                if openMenu then openMenu() end
                switchTab("Theme")
            end)
        end)
    end

    -- ===== TAB INFO =====
    local pInfo = Pages.Info

    UI.heading(pInfo, "📌 Thông tin Script")
    local infoCard = mk("Frame", pInfo, {
        Size = UDim2_new(1, 0, 0, 160),
        BackgroundColor3 = Theme.bgCard, BackgroundTransparency = 0.25,
        BorderSizePixel = 0, ZIndex = 11,
    })
    corner(infoCard, 8)
    stroke(infoCard, Theme.accent, 1, 0.6)

    mk("TextLabel", infoCard, {
        Size = UDim2_new(1, -16, 1, -12), Position = UDim2_new(0, 8, 0, 6),
        BackgroundTransparency = 1,
        Text = string_format(
            "⚡ BLACKZ VIP FLY v6.0 BYPASS\n" ..
            "━━━━━━━━━━━━━━━━━━\n" ..
            "👑 Tier: %s\n" ..
            "🔑 Key: %s\n" ..
            "🎮 Executor: %s\n" ..
            "🛡 Bypass: %s\n" ..
            "🕊️ Fly Mode: %s\n" ..
            "📊 Fly Speed: %d\n" ..
            "🚫 Anti-Kick: %s\n" ..
            "━━━━━━━━━━━━━━━━━━\n" ..
            "Keys: blackzfree / blackzmid / blackzpro",
            KeyState.tier, KeyState.key or "N/A", ExecutorInfo.name,
            BypassEngine.enabled and "ON" or "OFF",
            State.flyMode, State.flySpeed,
            BypassEngine.antiKickEnabled and "ON" or "OFF"
        ),
        TextColor3 = Theme.txt, Font = Enum.Font.Code,
        TextSize = 9, TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top, TextWrapped = true, ZIndex = 12,
    })

    UI.heading(pInfo, "⌨️ Phím tắt")
    local keysCard = mk("Frame", pInfo, {
        Size = UDim2_new(1, 0, 0, 130),
        BackgroundColor3 = Theme.bgCard, BackgroundTransparency = 0.25,
        BorderSizePixel = 0, ZIndex = 11,
    })
    corner(keysCard, 8)
    stroke(keysCard, Theme.accent, 1, 0.6)
    mk("TextLabel", keysCard, {
        Size = UDim2_new(1, -16, 1, -12), Position = UDim2_new(0, 8, 0, 6),
        BackgroundTransparency = 1,
        Text = "RightShift: Mở/đóng menu\nP: Bật/tắt Fly\nK: Panic Button\nF: Toggle HUD\nV: Invisible (VIP)\nB: Spinbot (VIP)\nT: Cycle TP | L: Player List\nC: Crosshair\nF2/F3: Save/Load Pos\nSpace/LShift: Lên/Xuống",
        TextColor3 = Theme.sub, Font = Enum.Font.Code,
        TextSize = 9, TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top, ZIndex = 12,
    })

    -- ===== Window controls =====
    local function openMenuFn()
        State.uiOpen = true
        Window.Visible = true
        Window.Size = UDim2_new(0, WIN_W * 0.9, 0, WIN_H * 0.9)
        tween(Window, 0.3, { Size = UDim2_new(0, WIN_W, 0, WIN_H) })
    end

    local function closeMenuFn()
        State.uiOpen = false
        tween(Window, 0.25, { Size = UDim2_new(0, WIN_W * 0.9, 0, WIN_H * 0.9) })
        task_wait(0.25)
        Window.Visible = false
    end

    openMenu = openMenuFn
    closeMenu = closeMenuFn

    MinBtn.MouseButton1Click:Connect(function()
        if State.uiMinimized then
            State.uiMinimized = false
            tween(Window, 0.3, { Size = UDim2_new(0, WIN_W, 0, WIN_H) })
            Sidebar.Visible = true
            Content.Visible = true
        else
            State.uiMinimized = true
            tween(Window, 0.3, { Size = UDim2_new(0, WIN_W, 0, 32) })
            Sidebar.Visible = false
            Content.Visible = false
        end
    end)

    CloseBtn.MouseButton1Click:Connect(closeMenuFn)

    local dragging, dragStart, winStart
    TitleBar.InputBegan:Connect(function(input)
        if input.UserInputType == UIT.MouseButton1 or input.UserInputType == UIT.Touch then
            dragging = true
            dragStart = input.Position
            winStart = Window.Position
        end
    end)
    TitleBar.InputEnded:Connect(function() dragging = false end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == UIT.MouseMovement or input.UserInputType == UIT.Touch) then
            local delta = input.Position - dragStart
            Window.Position = UDim2_new(
                winStart.X.Scale, winStart.X.Offset + delta.X,
                winStart.Y.Scale, winStart.Y.Offset + delta.Y
            )
        end
    end)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 19: FLOATING BUTTONS                                                   ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
FloatGui = mk("ScreenGui", nil, {
    Name = "BlackZ_Floats_" .. tostring(math_random(100000, 999999)),
    ResetOnSpawn = false, IgnoreGuiInset = true,
    DisplayOrder = 99989,
})
SafeParent(FloatGui)

MakeFloatBtn = function(icon, yPos, callback, color)
    local btn = mk("TextButton", FloatGui, {
        Size = UDim2_new(0, 40, 0, 40),
        Position = UDim2_new(1, -50, 0, yPos),
        BackgroundColor3 = Theme.bg, BackgroundTransparency = 0.1,
        Text = icon, TextSize = 16, Font = Enum.Font.GothamBold,
        TextColor3 = color or Theme.accent,
        AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 998,
    })
    corner(btn, 999)
    local s = stroke(btn, color or Theme.accent, 2, 0.3)
    gradient(s, Theme.accent, Theme.accent2, 45)

    task_spawn(function()
        local phase = 0
        while btn.Parent do
            phase = phase + 0.08
            local st = btn:FindFirstChildOfClass("UIStroke")
            if st then
                pcall(function() st.Transparency = 0.3 + 0.3 * math_abs(math_sin(phase)) end)
            end
            task_wait(0.05)
        end
    end)

    btn.MouseButton1Click:Connect(function() pcall(callback) end)

    local dragging, dragStart, startPos
    btn.InputBegan:Connect(function(input)
        if input.UserInputType == UIT.MouseButton1 or input.UserInputType == UIT.Touch then
            dragging = true
            dragStart = input.Position
            startPos = btn.Position
        end
    end)
    btn.InputEnded:Connect(function(input)
        if input.UserInputType == UIT.MouseButton1 or input.UserInputType == UIT.Touch then
            dragging = false
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == UIT.MouseMovement or input.UserInputType == UIT.Touch) then
            local delta = input.Position - dragStart
            btn.Position = UDim2_new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    return btn
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 20: KEY UI                                                             ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local function BuildKeyUI()
    local KeyGui = mk("ScreenGui", nil, {
        Name = "BlackZ_Key_" .. tostring(math_random(100000, 999999)),
        ResetOnSpawn = false, IgnoreGuiInset = true,
        DisplayOrder = 99999,
    })
    SafeParent(KeyGui)

    local Card = mk("Frame", KeyGui, {
        Size = UDim2_new(0, 300, 0, 230),
        Position = UDim2_new(0.5, -150, 0.5, -115),
        BackgroundColor3 = Theme.bg, BorderSizePixel = 0,
    })
    corner(Card, 12)
    local cs = stroke(Card, Theme.accent, 2, 0.3)
    gradient(cs, Theme.accent, Theme.accent2, 45)

    local bgGrad = mk("Frame", Card, {
        Size = UDim2_new(1, 0, 1, 0),
        BackgroundColor3 = Theme.bgMid, BackgroundTransparency = 0.6,
        BorderSizePixel = 0,
    })
    local bgg = gradient(bgGrad, Theme.bg, Color3_RGB(0, 40, 60), 135)
    task_spawn(function()
        local ang = 0
        while bgGrad.Parent do
            ang = (ang + 0.5) % 360
            pcall(function() bgg.Rotation = ang end)
            task_wait(0.05)
        end
    end)

    local Title = mk("TextLabel", Card, {
        Size = UDim2_new(1, -20, 0, 30),
        Position = UDim2_new(0, 10, 0, 14),
        BackgroundTransparency = 1, Text = "⚡ BLACKZ v6.0 BYPASS",
        Font = Enum.Font.GothamBlack, TextSize = 18,
        TextXAlignment = Enum.TextXAlignment.Center,
    })
    gradient(Title, Theme.accent, Theme.accent2, 0)

    mk("TextLabel", Card, {
        Size = UDim2_new(1, -20, 0, 16), Position = UDim2_new(0, 10, 0, 48),
        BackgroundTransparency = 1, Text = "Nhập key để truy cập",
        TextColor3 = Theme.sub, Font = Enum.Font.GothamMedium,
        TextSize = 10, TextXAlignment = Enum.TextXAlignment.Center,
    })

    local KeyBoxFrame = mk("Frame", Card, {
        Size = UDim2_new(1, -28, 0, 40), Position = UDim2_new(0, 14, 0, 76),
        BackgroundColor3 = Theme.bgCard, BorderSizePixel = 0,
    })
    corner(KeyBoxFrame, 8)
    stroke(KeyBoxFrame, Theme.accent, 1.5, 0.4)

    local KeyBox = mk("TextBox", KeyBoxFrame, {
        Size = UDim2_new(1, -16, 1, 0), Position = UDim2_new(0, 8, 0, 0),
        BackgroundTransparency = 1, Text = "",
        PlaceholderText = "blackzfree / blackzmid / blackzpro",
        TextColor3 = Theme.txt, PlaceholderColor3 = Theme.sub,
        Font = Enum.Font.GothamSemibold, TextSize = 11, ClearTextOnFocus = false,
    })

    local SubmitBtn = mk("TextButton", Card, {
        Size = UDim2_new(1, -28, 0, 40), Position = UDim2_new(0, 14, 0, 126),
        BackgroundColor3 = Theme.accent, Text = "🔓 XÁC NHẬN KEY",
        TextColor3 = Color3_RGB(10, 10, 15), Font = Enum.Font.GothamBlack,
        TextSize = 12, AutoButtonColor = false, BorderSizePixel = 0,
    })
    corner(SubmitBtn, 8)
    gradient(SubmitBtn, Theme.accent, Theme.accent2, 90)

    local MsgLabel = mk("TextLabel", Card, {
        Size = UDim2_new(1, -20, 0, 30), Position = UDim2_new(0, 10, 0, 176),
        BackgroundTransparency = 1, Text = "", TextColor3 = Theme.sub,
        Font = Enum.Font.GothamMedium, TextSize = 9,
        TextXAlignment = Enum.TextXAlignment.Center, TextWrapped = true,
    })

    local function TrySubmit()
        local k = KeyBox.Text
        local ok, msg = VerifyKey(k)
        if ok then
            MsgLabel.Text = "✅ " .. msg .. " — " .. KeyState.tier
            MsgLabel.TextColor3 = Color3_RGB(80, 255, 120)
            task_wait(0.6)
            pcall(function() KeyGui:Destroy() end)
            SafeNotify("✅ Xác thực OK", "Tier: " .. KeyState.tier, 3)

            -- Khởi tạo bypass
            EnableAllBypass()
            
            BuildUI()
            UpdateTierBadge()
            CreateHJoystick()
            CreateVJoystick()

            MakeFloatBtn("⚡", 15, function()
                if State.uiOpen then
                    if closeMenu then closeMenu() end
                else
                    if openMenu then openMenu() end
                end
            end, Theme.accent)
            MakeFloatBtn("🕊", 60, function() ToggleFly() end, Color3_RGB(80, 255, 120))
            MakeFloatBtn("🛡", 105, function()
                BypassEngine.enabled = not BypassEngine.enabled
                if BypassEngine.enabled then EnableAllBypass() else DisableAllBypass() end
            end, Color3_RGB(100, 200, 255))
            MakeFloatBtn("🚨", 150, function()
                if closeMenu then closeMenu() end
                if State.flyEnabled then StopFly() end
                if VJoyGui then VJoyGui.Enabled = false end
                SafeNotify("🚨 Panic", "Đã ẩn & tắt", 2)
            end, Color3_RGB(255, 60, 60))

            task_wait(0.3)
            if openMenu then openMenu() end
        else
            MsgLabel.Text = "❌ " .. msg
            MsgLabel.TextColor3 = Color3_RGB(255, 60, 90)
            KeyBox.Text = ""
        end
    end

    SubmitBtn.MouseButton1Click:Connect(TrySubmit)
    KeyBox.FocusLost:Connect(function(enter) if enter then TrySubmit() end end)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 21: HUD SYSTEM                                                         ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
HudGui = mk("ScreenGui", nil, {
    Name = "BlackZ_HUD_" .. tostring(math_random(100000, 999999)),
    ResetOnSpawn = false, IgnoreGuiInset = true,
    DisplayOrder = 99980, ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
})
SafeParent(HudGui)

local HudFrame = mk("Frame", HudGui, {
    Size = UDim2_new(0, 230, 0, 78),
    Position = UDim2_new(0, 12, 0, 12),
    BackgroundColor3 = Theme.bg, BackgroundTransparency = 0.15,
    BorderSizePixel = 0, ZIndex = 99980,
})
corner(HudFrame, 8)
stroke(HudFrame, Theme.accent, 1.5, 0.35)

local HudTitle = mk("TextLabel", HudFrame, {
    Size = UDim2_new(1, -16, 0, 14), Position = UDim2_new(0, 8, 0, 4),
    BackgroundTransparency = 1, Text = "⚡ BLACKZ v6.0 BYPASS",
    Font = Enum.Font.GothamBlack, TextSize = 10,
    TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 99981,
})
gradient(HudTitle, Theme.accent, Theme.accent2, 0)

local HudInfo = mk("TextLabel", HudFrame, {
    Size = UDim2_new(1, -16, 1, -22), Position = UDim2_new(0, 8, 0, 20),
    BackgroundTransparency = 1, Text = "FPS: -- | Ping: -- ms",
    TextColor3 = Theme.sub, Font = Enum.Font.Code, TextSize = 9,
    TextXAlignment = Enum.TextXAlignment.Left,
    TextYAlignment = Enum.TextYAlignment.Top,
    TextWrapped = true, ZIndex = 99981,
})

task_spawn(function()
    local lastPing = 0
    while HudFrame.Parent do
        task_wait(0.5)
        if not State.hudEnabled then
            HudFrame.Visible = false
        else
            HudFrame.Visible = true
            local fps = 0
            pcall(function()
                fps = math_floor(Stats.FrameTime > 0 and (1 / Stats.FrameTime) or 0)
            end)
            pcall(function()
                local pingStat = Stats.Network.ServerStatsItem["Data Ping"]
                if pingStat then lastPing = math_floor(pingStat:GetValue()) end
            end)
            local posStr = "N/A"
            if State.hrp then
                local p = State.hrp.Position
                posStr = string_format("%d, %d, %d", math_floor(p.X), math_floor(p.Y), math_floor(p.Z))
            end
            HudInfo.Text = string_format(
                "FPS: %d | Ping: %d ms\nPos: %s\n🛡 Bypass: %s | Tier: %s",
                fps, lastPing, posStr,
                BypassEngine.enabled and "ON" or "OFF", KeyState.tier
            )
        end
    end
end)

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 22: CROSSHAIR + PLAYER LIST                                            ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
CrosshairGui = mk("ScreenGui", nil, {
    Name = "BlackZ_Crosshair_" .. tostring(math_random(100000, 999999)),
    ResetOnSpawn = false, IgnoreGuiInset = true, DisplayOrder = 99970,
})
SafeParent(CrosshairGui)
CrosshairGui.Enabled = false

local CrosshairDot = mk("Frame", CrosshairGui, {
    Size = UDim2_new(0, 6, 0, 6),
    Position = UDim2_new(0.5, -3, 0.5, -3),
    BackgroundColor3 = Color3_RGB(255, 50, 50),
    BorderSizePixel = 0, ZIndex = 99970,
})
corner(CrosshairDot, 999)
stroke(CrosshairDot, Color3_new(1, 1, 1), 1, 0.3)

local function CreatePlayerList()
    if PlayerListGui then return end
    PlayerListGui = mk("ScreenGui", nil, {
        Name = "BlackZ_PlayerList_" .. tostring(math_random(100000, 999999)),
        ResetOnSpawn = false, IgnoreGuiInset = true, DisplayOrder = 99975,
    })
    SafeParent(PlayerListGui)
    PlayerListGui.Enabled = false

    local frame = mk("Frame", PlayerListGui, {
        Size = UDim2_new(0, 200, 0, 260),
        Position = UDim2_new(0, 12, 0.5, -130),
        BackgroundColor3 = Theme.bg, BackgroundTransparency = 0.1,
        BorderSizePixel = 0, ZIndex = 99975,
    })
    corner(frame, 8)
    stroke(frame, Theme.accent, 1.5, 0.35)

    mk("TextLabel", frame, {
        Size = UDim2_new(1, -16, 0, 20), Position = UDim2_new(0, 8, 0, 6),
        BackgroundTransparency = 1, Text = "👥 Người chơi",
        TextColor3 = Theme.txt, Font = Enum.Font.GothamBlack,
        TextSize = 11, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 99976,
    })

    local scroll = mk("ScrollingFrame", frame, {
        Size = UDim2_new(1, -16, 1, -34), Position = UDim2_new(0, 8, 0, 28),
        BackgroundTransparency = 1, ScrollBarThickness = 2,
        ScrollBarImageColor3 = Theme.accent,
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        CanvasSize = UDim2_new(0, 0, 0, 0),
        BorderSizePixel = 0, ZIndex = 99976,
    })
    listLayout(scroll, 3)

    local function refresh()
        if not scroll.Parent then return end
        for _, c in ipairs(scroll:GetChildren()) do
            if c:IsA("TextButton") then c:Destroy() end
        end
        for _, p in ipairs(Players:GetPlayers()) do
            local btn = UI.gridButton(scroll, "🎯 " .. p.Name, Theme.bgCard, function()
                if not GetTierConfig().allowTeleport then
                    Notify("error", "VIP Only", "Cần VIP", 3); return
                end
                if p.Character and p.Character:FindFirstChild("HumanoidRootPart") and State.hrp then
                    State.hrp.CFrame = p.Character.HumanoidRootPart.CFrame * CFrame_new(0, 3, 0)
                    Notify("success", "TP", "Đến " .. p.Name, 2)
                end
            end)
            btn.Size = UDim2_new(1, 0, 0, 24)
        end
    end

    task_spawn(function()
        while scroll.Parent do
            task_wait(3)
            refresh()
        end
    end)
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 23: MAIN LOOPS                                                         ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝

-- Character refresh
task_spawn(function()
    while true do
        task_wait(0.5)
        UpdateChar()
    end
end)

-- WalkSpeed / JumpPower enforcement (chỉ khi không bypass)
task_spawn(function()
    while true do
        task_wait(0.2)
        pcall(function()
            if State.hum and State.hum.Health > 0 then
                if not speedBypassEnabled then
                    local spd = State.walkSpeed or 50
                    if State.speedBoost and State.hum.MoveDirection.Magnitude > 0 then
                        spd = spd * (State.speedBoostMul or 2)
                    end
                    if State.hum.WalkSpeed ~= spd then State.hum.WalkSpeed = spd end
                end
                if State.jumpPower and State.hum.JumpPower ~= State.jumpPower then
                    State.hum.JumpPower = State.jumpPower
                end
            end
        end)
    end
end)

-- Noclip enforcement
task_spawn(function()
    while true do
        task_wait(0.1)
        pcall(function()
            if State.noclip and State.char then
                for _, p in ipairs(State.char:GetDescendants()) do
                    if p:IsA("BasePart") and p.CanCollide then p.CanCollide = false end
                end
            end
        end)
    end
end)

-- Infinite Jump
UserInputService.JumpRequest:Connect(function()
    if State.infiniteJump and State.hum then
        pcall(function() State.hum:ChangeState(Enum.HumanoidStateType.Jumping) end)
    end
end)

-- Anti-AFK
task_spawn(function()
    while true do
        task_wait(60)
        if State.antiAFK then
            pcall(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2_new(0, 0))
            end)
        end
    end
end)

-- Auto Rejoin
task_spawn(function()
    while true do
        task_wait(5)
        if State.autoRejoin then
            pcall(function()
                if not LP.Parent then TeleportService:Teleport(game.PlaceId, LP) end
            end)
        end
    end
end)

-- GC Cleanup
task_spawn(function()
    while true do
        task_wait(120)
        pcall(function() collectgarbage("collect") end)
    end
end)

-- Spinbot
task_spawn(function()
    while true do
        task_wait(0.02)
        if State.spinbot and State.hrp then
            pcall(function()
                local speed = (State.spinbotSpeed or 20) * 0.02
                State.hrp.CFrame = State.hrp.CFrame * CFrame_Angles(0, math_rad(speed * 60), 0)
            end)
        end
    end
end)

-- Character Spin
task_spawn(function()
    while true do
        task_wait(0.05)
        if State.characterSpin and State.hrp then
            pcall(function()
                local speed = (State.characterSpinSpeed or 10) * 0.05
                State.hrp.CFrame = State.hrp.CFrame * CFrame_Angles(0, math_rad(speed * 20), 0)
            end)
        end
    end
end)

-- Kill Aura
task_spawn(function()
    while true do
        task_wait(0.3)
        if State.killAura and State.hrp then
            pcall(function()
                for _, p in ipairs(Players:GetPlayers()) do
                    if p ~= LP and p.Character then
                        local hrp = p.Character:FindFirstChild("HumanoidRootPart")
                        local hum = p.Character:FindFirstChildOfClass("Humanoid")
                        if hrp and hum and hum.Health > 0 then
                            local dist = (hrp.Position - State.hrp.Position).Magnitude
                            if dist <= (State.killAuraRange or 20) then hum.Health = 0 end
                        end
                    end
                end
            end)
        end
    end
end)

-- Hitbox Expander
task_spawn(function()
    while true do
        task_wait(0.5)
        if State.hitboxExpand then
            pcall(function()
                for _, p in ipairs(Players:GetPlayers()) do
                    if p ~= LP and p.Character then
                        local hrp = p.Character:FindFirstChild("HumanoidRootPart")
                        if hrp then
                            hrp.Size = Vector3_new(State.hitboxSize or 10, State.hitboxSize or 10, State.hitboxSize or 10)
                            hrp.Transparency = 1
                            hrp.CanCollide = false
                        end
                    end
                end
            end)
        end
    end
end)

-- Aimbot
task_spawn(function()
    while true do
        task_wait(0.05)
        if State.aimbot then
            pcall(function()
                local closest, minDist = nil, State.aimbotRadius or 200
                for _, p in ipairs(Players:GetPlayers()) do
                    if p ~= LP and p.Character then
                        local targetPart = p.Character:FindFirstChild(State.aimbotTarget or "Head")
                        local hum = p.Character:FindFirstChildOfClass("Humanoid")
                        if targetPart and hum and hum.Health > 0 and State.hrp then
                            local dist = (targetPart.Position - State.hrp.Position).Magnitude
                            if dist < minDist then minDist = dist; closest = targetPart end
                        end
                    end
                end
                if closest then
                    Camera.CFrame = CFrame_new(Camera.CFrame.Position, closest.Position)
                end
            end)
        end
    end
end)

-- No Fall Damage
task_spawn(function()
    while true do
        task_wait(0.5)
        if State.noFallDamage and State.hum then
            pcall(function() State.hum:SetStateEnabled(HS.FallingDown, false) end)
        end
    end
end)

-- Godmode
task_spawn(function()
    while true do
        task_wait(0.1)
        if State.godmode and State.hum then
            pcall(function()
                if State.hum.Health < State.hum.MaxHealth then
                    State.hum.Health = State.hum.MaxHealth
                end
            end)
        end
    end
end)

-- X-Ray
task_spawn(function()
    while true do
        task_wait(2)
        if State.xray then
            pcall(function()
                for _, obj in ipairs(Workspace:GetDescendants()) do
                    if obj:IsA("BasePart") and not obj:IsDescendantOf(LP.Character) then
                        local isPlayer = false
                        for _, p in ipairs(Players:GetPlayers()) do
                            if p.Character and obj:IsDescendantOf(p.Character) then
                                isPlayer = true; break
                            end
                        end
                        if not isPlayer then obj.Transparency = 0.6 end
                    end
                end
            end)
        end
    end
end)

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 24: TP CYCLE                                                           ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local TPCycle = { index = 1 }

local function CycleTPNext()
    local players = Players:GetPlayers()
    local valid = {}
    for _, p in ipairs(players) do
        if p ~= LP and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            table_insert(valid, p)
        end
    end
    if #valid == 0 then
        Notify("warn", "TP", "Không có ai để TP", 2); return
    end
    if TPCycle.index > #valid then TPCycle.index = 1 end
    local target = valid[TPCycle.index]
    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") and State.hrp then
        State.hrp.CFrame = target.Character.HumanoidRootPart.CFrame * CFrame_new(0, 3, 0)
        Notify("success", "TP", "Đến " .. target.Name, 2)
    end
    TPCycle.index = TPCycle.index + 1
end

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 25: HOTKEYS                                                            ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if UserInputService:GetFocusedTextBox() then return end

    if input.KeyCode == KS.RightShift then
        if not KeyState.verified then
            SafeNotify("⚠️ Chưa xác thực", "Nhập key trước", 2); return
        end
        if State.uiOpen then
            if closeMenu then closeMenu() end
        else
            if openMenu then openMenu() end
        end
    elseif input.KeyCode == KS.P then
        if KeyState.verified then ToggleFly() end
    elseif input.KeyCode == KS.K then
        if KeyState.verified then
            if closeMenu then closeMenu() end
            if State.flyEnabled then StopFly() end
            if VJoyGui then VJoyGui.Enabled = false end
            if HJoyGui then HJoyGui.Enabled = false end
            SafeNotify("🚨 Panic", "Đã ẩn menu & tắt features", 2)
        end
    elseif input.KeyCode == KS.F then
        if KeyState.verified and FloatGui then
            FloatGui.Enabled = not FloatGui.Enabled
        end
    elseif input.KeyCode == KS.Space then
        if State.flyEnabled then
            State.vjoyActive = true
            State.vjoyValue = 1
        end
    elseif input.KeyCode == KS.LeftShift then
        if State.flyEnabled then
            State.vjoyActive = true
            State.vjoyValue = -1
        end
    elseif input.KeyCode == KS.F2 then
        if KeyState.verified and State.hrp then
            _G.BlackZ_SavedPos = State.hrp.Position
            Notify("success", "Save Pos", "Đã lưu vị trí", 2)
        end
    elseif input.KeyCode == KS.F3 then
        if KeyState.verified and _G.BlackZ_SavedPos and State.hrp then
            State.hrp.CFrame = CFrame_new(_G.BlackZ_SavedPos + Vector3_new(0, 3, 0))
            Notify("success", "Load Pos", "Về vị trí đã lưu", 2)
        end
    elseif input.KeyCode == KS.T then
        if KeyState.verified then CycleTPNext() end
    elseif input.KeyCode == KS.V then
        if KeyState.verified and GetTierConfig().allowInvisible then
            State.invisible = not State.invisible
            ApplyInvisible(State.invisible)
            if State.invisible then
                EnableInvisibleLoop()
                Notify("success", "👻 Invisible ON", "", 2)
            else
                DisableInvisibleLoop()
                Notify("warn", "👻 Invisible OFF", "", 2)
            end
        end
    elseif input.KeyCode == KS.B then
        if KeyState.verified and GetTierConfig().allowSpinbot then
            State.spinbot = not State.spinbot
            Notify(State.spinbot and "success" or "warn", "🔄 Spinbot", State.spinbot and "ON" or "OFF", 2)
        end
    elseif input.KeyCode == KS.H then
        State.hudEnabled = not State.hudEnabled
        Notify("info", "HUD", State.hudEnabled and "ON" or "OFF", 1)
    elseif input.KeyCode == KS.L then
        if not PlayerListGui then CreatePlayerList() end
        if PlayerListGui then PlayerListGui.Enabled = not PlayerListGui.Enabled end
    elseif input.KeyCode == KS.C then
        CrosshairGui.Enabled = not CrosshairGui.Enabled
    end
end)

UserInputService.InputEnded:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == KS.Space or input.KeyCode == KS.LeftShift then
        if State.flyEnabled then
            State.vjoyActive = false
            State.vjoyValue = 0
        end
    end
end)

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 26: SAFE DESTRUCTOR                                                    ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
_G.BlackZ_Alive = true

task_spawn(function()
    while _G.BlackZ_Alive do
        task_wait(1)
    end
    local targets = { MainGui, NotifyGui, VJoyGui, HJoyGui, FloatGui, HudGui, PlayerListGui, CrosshairGui, ESPContainer }
    for _, g in ipairs(targets) do
        pcall(function() if g then g:Destroy() end end)
    end
end)

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 27: BOOT SEQUENCE                                                      ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
local function Boot()
    local ok, err = xpcall(function()
        BuildKeyUI()
        SafeNotify("⚡ BLACKZ v6.0 BYPASS", "Nhập key để bắt đầu", 5)
    end, function(e)
        return tostring(e) .. "\n" .. debug.traceback()
    end)
    if not ok then
        warn("[BLACKZ] Boot error: " .. tostring(err))
    end
end

task_spawn(Boot)

-- ╔══════════════════════════════════════════════════════════════════════════════╗
-- ║ MODULE 28: CONSOLE BANNER                                                     ║
-- ╚══════════════════════════════════════════════════════════════════════════════╝
print("═══════════════════════════════════════════════════════════════")
print("⚡ BLACKZ VIP FLY v6.0 — BYPASS EDITION")
print("═══════════════════════════════════════════════════════════════")
print("  🔑 Keys: blackzfree | blackzmid | blackzpro")
print("  🛡️ BYPASS ENGINE:")
print("     • Anti-Kick (Kick method + Remote block)")
print("     • Bypass Speed (CFrame-based)")
print("     • Bypass Fly (CFrame-based, no BodyVelocity)")
print("     • Property Spoofing (WalkSpeed/JumpPower)")
print("     • Remote Event Blocking")
print("     • Anti-Cheat Script Disabling")
print("  ✈️ Fly Modes: CFrameBypass / BodyVelocity")
print("     LinearVelocity / CFrame / Camera")
print("     Behind / Tween")
print("  👻 Invisible: 100% full")
print("  🔄 Spinbot / Char-Spin / Kill-Aura / Aimbot")
print("  📡 ESP Highlight + Names + Health")
print("  🌀 Teleport: Random / Saved / Player List")
print("  ⌨️  Hotkeys:")
print("     RightShift → Menu | P → Fly | K → Panic")
print("     F → Floats | T → Cycle-TP | V → Invisible")
print("     B → Spinbot | H → HUD | L → PlayerList")
print("     C → Crosshair | F2/F3 → Save/Load Pos")
print("     Space / LShift → Fly Up / Down")
print("═══════════════════════════════════════════════════════════════")
print("  ✅ Script loaded — waiting for key...")
print("═══════════════════════════════════════════════════════════════")